# WO — ASTRA-C1-SEMANTIC-HELDOUT-01

Auditor 20260907T1455Z ACCEPT_PARTIAL 16k formulaic grid. REJECT 800k.
ONE UNKNOWN: held-out gold — queries whose surface form is NOT the index fill template `{ent} {rel} {ent}`, independent labels, still retrieve labeled nids under same context-02 + stream-02 instantiate.

Do NOT write independent tree. Do NOT 800k. Do NOT 12-entity clone.
N can stay 16384 or smaller if XSim bound — state N in PREREG. C1 800k OPEN.

Gold must not be the cartesian generator used in SEMANTIC-16K fill. Paraphrase / role-reversal / distractor excluded set required. SEARCH_INCOMPLETE on gold_n>=1 retrieve = FAIL.
Instantiate KEEP ctx DUT 8255a798 / keys 124be808; named lexicon law qse-v2-lex-semantic-16k-01 (df0e8833) if needed — do not silent C0 shadow undocumented.
leftover A09 off poke_v=0 PROGRAM=NO.

Bag: results/A7-NATIVE-GRAPH/ASTRA-C1-SEMANTIC-HELDOUT-01/
Marker ASTRA_C1_SEMANTIC_HELDOUT_XSIM_PASS only if FAIL=0 AND at least one paraphrase gold hits AND fill-template query still hits control gold.
Gold hash BEFORE xvlog.
