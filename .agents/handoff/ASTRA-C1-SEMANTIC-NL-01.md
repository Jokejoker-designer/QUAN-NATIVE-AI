# WO — ASTRA-C1-SEMANTIC-NL-01

Auditor 20260907T1835Z ACCEPT_PARTIAL 8/8 off-grid. REJECT 800k. Next OPTIONAL true NL, not 800k.

ONE UNKNOWN: a query whose **bound keys after QSE are NOT identical** to the fill-template control `"boiler feeds header"` (k0=3332 k1=3588), yet still retrieves the labeled gold nids for that meaning — OR FAIL honestly if C0 QSE cannot (do not CLS_SKIP-only paraphrase; that was HELDOUT).

Do NOT write independent tree. C1 800k OPEN. BOARD_PASS not claimed.
Instantiate 124be808 + 8255a798 not edited. leftover A09 off poke_v=0 PROGRAM=NO.
N may be 16384 copy KEEP corpus. Gold hash BEFORE xvlog.
If no such query exists under frozen extract, RESULT=FAIL this unknown (NOT_SELECTIVE_NL), do not fake keys.

Bag: results/A7-NATIVE-GRAPH/ASTRA-C1-SEMANTIC-NL-01/
Marker ASTRA_C1_SEMANTIC_NL_XSIM_PASS only if keys_match=0 vs fill-template AND gold HIT.
