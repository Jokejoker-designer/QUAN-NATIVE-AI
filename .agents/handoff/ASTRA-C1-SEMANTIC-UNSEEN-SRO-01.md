# WO — ASTRA-C1-SEMANTIC-UNSEEN-SRO-01

Owner resumed (`tiếp tục`). Auditor 20260907T1550Z ACCEPT_PARTIAL HELDOUT (surface-only QSE skip).
Parent next: **unseen SRO / non-grid**, not 800k.

ONE UNKNOWN: at least one labeled fact whose (subj,rel,obj) is **not** in the cartesian `{ent}{rel}{ent}` fill used for bulk 16k, is retrieved by a query whose bound SRO matches that fact; and a query for a **held-out combination that is not indexed** must not emit fill-grid neighbors (empty or UNKNOWN, not `{120,121,122}`).

Do NOT write independent tree. C1 800k OPEN. BOARD_PASS not claimed.
Instantiate KEEP ctx 124be808 + DUT 8255a798 not edited. Named lexicon df0e8833 copy ok if documented.
leftover A09 off poke_v=0 PROGRAM=NO. SEARCH_INCOMPLETE on gold_n>=1 retrieve = FAIL.

Bag: results/A7-NATIVE-GRAPH/ASTRA-C1-SEMANTIC-UNSEEN-SRO-01/
Marker ASTRA_C1_SEMANTIC_UNSEEN_SRO_XSIM_PASS only if FAIL=0.
Gold hash BEFORE xvlog.
