# R8 design contract — trạng thái + responsive/a11y

| Field | Decision |
| --- | --- |
| Screen job | Show honest empty/error/disconnected states; keep 1280 usable via drawers; make every metric reachable by keyboard. |
| Primary user and action | Notice FPGA is off-line; open recorded #1842 or stay on Twin; open empty #1841 wave without a fake draw. |
| Content hierarchy | Disconnected banner first. Empty wave uses SPEC title. Insight is a right drawer below 1440 px. |
| Navigation | `[` / `]` cycle every tab. Skip link to `#gb-main`. Waveform cursor is a slider. |
| Visual language | StudioState on Card. Hairline SYNTHETIC. No BOARD fill. |
| Required states | disconnected, no-waveform (1841), partial-trace (1841), loading (hydrate), overflow/timing only when recorded. |
| Evidence | Fixture 1841 CAPTURE_DISABLED. UIZZE catalogue was not browsed this turn. |
| Forbidden | LiteScope arm; fake live KPIs; inert “Bật capture” with no outcome. |
| Acceptance | Banner + Mở session → Replay. 1841 wave empty + toast. `]` reaches Bằng chứng. 1280 drawer / 1440 rail. Table shows update 512. |
