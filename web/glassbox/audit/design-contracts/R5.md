# R5 design contract — Đầu ra + Sóng FPGA

| Field | Decision |
| --- | --- |
| Screen job | Show how one recorded answer was assembled, then the fixture waveform for the same interaction. |
| Primary user and action | Click a token to see candidates + cycle; click an annotation to move the cursor. |
| Content hierarchy | Source pill → generated text / capture facts → timeline or canvas → inspector. |
| Navigation | Key 9 = Đầu ra. Waveform via sidebar. Trace-back opens episode/model/waveform facts, not a new silicon capture. |
| Visual language | Ranked bars for probabilities; digital canvas from RLE transitions. |
| Required states | No output events; waveform absence (1841 path exists in fixture). |
| Responsive | Token wrap; waveform sidebar stacks under 1280. |
| Evidence used | OutputEvent + WaveformCapture SYNTHETIC. SPEC §17–18. |
| Forbidden | BOARD/LiteScope pills, sin() traces, toast “Đã xuất VCD” without export.ts, percent on SCORE. |
| Acceptance | Artix 72% from PROBABILITY; SELECTED + cycle; annotations from capture; export uses downloadArtifact result. |
