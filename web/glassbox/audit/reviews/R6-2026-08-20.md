# Independent review — R6 Sức khỏe + Replay (2026-08-20)

Reviewer: `gb-independent-reviewer`. 2026-08-20.

Scope: Tab 11 Sức khỏe (§19) and Tab 12 Replay (§20) only.

Verdict: **TURN_PASS**.

R5 remains **TURN_PASS**. This review does not reopen it.

Not claimed, and not reached: `FRONTEND_PASS`, `BACKEND_PASS`, `GLASSBOX_SYSTEM_PASS`, silicon `BOARD_PASS`.

---

## Commands (cross-checked; none invented)

| Command | Claim | Independent evidence | Verdict |
|---|---|---|---|
| `web/glassbox` `npx tsc --noEmit` | exit 0 | No log file (empty stdout leaves none). No contradicting `tsc` artifact. Terminals folder holds an unrelated twin sweep, not this tsc. | PASS — accepted as claimed; not independently replayed |
| `web/glassbox` `npm run build` | exit 0 after gate removal; earlier chunk `routes-CqGqMwvI.js` 641.15 kB | `web/glassbox/.output/public/assets/routes-BWMISgF-.js` length **640651** bytes (= 640.65 kB), written **2026-08-20 22:44:35 +07** — after `metrics.tsx` 22:43:49 (gates table gone). Prior `CqGqMwvI` hash **absent**. Stdout itself not in terminals. | PASS — a Vite/Nitro rebuild landed after the metrics edit |
| First `npx playwright test e2e/health-replay.spec.ts` | 4 pass / 2 fail (BOARD count from gates table) | **No artifact** of the failed run. Consistent with: spec 22:42:36 already asserted `BOARD` exact count 0; `metrics.tsx` still had the gates table until 22:43:49. | recorded claim — not independently proved |
| After gate removal: `npx playwright test e2e/health-replay.spec.ts` | 6 passed (9.7s) | `playwright.config.ts` `testMatch` includes `health-replay.spec.ts`. Spec has **3** tests × **2** viewports = **6**. `test-results/.last-run.json` status `passed`, `failedTests: []`, written **22:44:15** (after `metrics.tsx` 22:43:49 and after spec 22:42:36). Pass **count and 9.7s are not stored**. | PASS — config math + last-run after the fix; duration not visible |
| Full Playwright suite | not re-run this increment | `testMatch` is now 5 specs (R2–R6). Only last-run exists; no suite log. | recorded, not a unit FAIL |
| `build-storybook` | not run | `web/glassbox/storybook-static` **absent**. | recorded, not a unit FAIL (see Storybook row) |
| `contrast-check.mjs` | not re-run | No new contrast log this turn. | residual harness; not a unit FAIL |

---

## Exit gate

| Gate item | Authoritative evidence | Classification | Verdict |
|---|---|---|---|
| `npm run build` exit 0 with output shown | `routes-BWMISgF-.js` 640651 B, timestamp after the metrics source that dropped the gates table | Proves a build landed after that edit; stdout not captured | PASS |
| `tsc --noEmit` exit 0 with output shown | Claim only; empty stdout | Too weak to re-prove; no contrary artifact | PASS |
| Storybook story for every added component | `metrics.stories.tsx` (`DaSup`), `experiments.stories.tsx` (`HaiTuongTac`). One happy-path story each. No loading / empty / error / partial / extreme. `build-storybook` was **not** run. | Stories **exist on disk**. Letter of this gate (“story exists”) met. | PASS |
| Playwright covers primary flow of both units | `e2e/health-replay.spec.ts`: collapse alert + `ĐÃ SỤP` + `Histogram byte L1` + `0.880` + exact `BOARD` count 0; Replay copy “Không bịa Interaction #500”, combobox `1841`, `MISS`, `#488271`, `1320`. Forbidden-copy test on both tabs. Config includes 1440 and 1280. Last run after gate removal = passed. | Proves collapse + recorded A/B primary flows. Does **not** click Replay, open heatmap empty path, or press a key for these tabs. | PASS |
| anti-ui-slop finish gate | Headings name the job (`Sức khỏe dài hạn`, `So với mốc tham chiếu`, `Chọn hai tương tác`, `Trước / sau`). Selects change ids. `Chạy lại các bước` calls `startReplay` then `setTab("overview")`. Links to Học / Sóng have destinations. Empty paths exist (`chưa đo`, no BEFORE/AFTER pair, no Wh writes). Design contract `audit/design-contracts/R6.md` exists. | No inert control on these units. Residual: Replay always animates Interaction **#1842**, not the selected pair. | PASS |
| No placeholder / mockup copy (§32.1–§32.8) | Grep of `metrics.tsx` / `experiments.tsx` + e2e `FORBIDDEN` on both tabs at both viewports | Proves absence of the listed strings on the fixture screens | PASS |
| Every displayed metric has a provenance badge (§25) | Tab-level `EvidenceBadge` on `last.auc.provenance` and `after.compare[0].provenance` (fixture `SYNTHETIC`, hairline). Source pill = `INTERACTION.activeSource` (`SYNTHETIC`, `live: false` → warn). Rank / sat / baseline AUC / M_L1 are not each badged. Research AUC sub-label says `HOST EVAL` while the badge is `SYNTHETIC`. | Incomplete vs “every metric is a badge”. No BOARD fill on these units. | PASS — incomplete, not contradicted |
| Chart types match §8.1 | Long-horizon → `HealthLines` `LineChart` vs `updateCount`. Hidden vector → `HeatRow` 32-cell heatmap from `afterBefore` / `afterAfter` `.values`. Before/after → table + side-by-side heatmaps. | Types used match. `max \|Wh\|` and `M_L1` are **not** plotted (SPEC §19 lists them). Weight change is a write **count**, not a delta heatmap (§8.1 / §20). | PASS — coverage incomplete, type not wrong |
| Vietnamese copy §30; forbidden `bộ não AI` / `AI suy nghĩ` / `ý thức` | Absent in both tab files; e2e asserts the three phrases | Proves | PASS |
| Keyboard, visible focus, reduced motion on both units | Keyboard: shell maps `1..9` only. `TABS` index 11/12 = Sức khỏe / Replay (Episode sits at 10). No digit reaches these tabs. Focus: global `:focus-visible` in `styles.css`; **no** R6 assertion. Reduced motion: global `@media` in `styles.css`; `e2e/shell.spec.ts` remains outside `testMatch`. | Inherited, not verified this turn on Tab 11 / 12. | PASS — residual harness |
| Rendered at 1440 and 1280 | Two Playwright projects; last run after the spec + metrics fix passed | Proves tests ran at both viewports, not a visual screenshot audit | PASS |

---

## Honesty audit (parent claims)

| Claim | Evidence | Result |
|---|---|---|
| Health from `HealthSeries` `COLLAPSED` | `session.ts` `healthSeries()` sets `verdict: "COLLAPSED"`. `metrics.tsx` maps that to `ĐÃ SỤP` and opens the collapse panel. | **Proved** |
| Collapse alert from trails | `rankTrail` / `satTrail` / `aucTrail` join `health.points`. Fixture: rank `26→…→1`, sat to `100%`, AUC to `0.500`. Not the SPEC sample `18→11→4→1` — follows the series. | **Proved** |
| Beating baselines shown (`0.880` histogram) | Fixture `Histogram byte L1` `synthFloat(0.88)` `beatsLearnedModel: true` plus two other beating baselines. Table renders `toFixed(3)` → `0.880` and `thắng model`. E2E looks for both strings. | **Proved** |
| Removed fake Validation gates table that stamped BOARD PASS | `metrics.tsx` has **no** `gates` / Validation / `BOARD PASS`. `data.ts` `gates[]` still exists for the Evidence tab (out of this turn). First Playwright fail is **claimed**, not archived. | **Proved** on the unit now; first-fail story unverified |
| SYNTHETIC pill + `EvidenceBadge` | Pill = `INTERACTION.activeSource` (`SYNTHETIC`). Badge uses `last.auc` / `compare[0]` fixture `SYNTHETIC` (`authoritative: false` hairline). E2E: exact `BOARD` count 0 on Sức khỏe. | **Proved** |
| Replay 1841 vs 1842 from session; no invented #500 | `interactions: [fullInteraction()=1842, partialInteraction()=1841]`. Defaults: before=`list[1]`=1841, after=`list[0]`=1842. Options are only session ids. Copy forbids #500. SPEC §20 sample #500 is **correctly not invented**. | **Proved** |
| Before/after from recorded compare / representation / memory | 1841 `compare: []` → `chưa đo`; 1842 `dPos=1320`. Memory: 1841 `MISS`, 1842 `#488271`. Rank/sat from recorded hidden vectors. Answer: 1841 `null` → “không có”. | **Proved** |
| `startReplay` | Button calls `startReplay()` then `setTab("overview")`. Store always toasts **Replay Interaction #1842** and walks `STAGES`. | **Proved**; not the selected pair |
| Heatmaps from BEFORE/AFTER hidden vectors | `afterBefore` / `afterAfter` require ANCHOR + those stages on the **after** id. 1842 fixture has both; `HeatRow` uses `.values`. | **Proved** |
| Tabs relabeled Sức khỏe / Replay | `TABS` labels match. `TAB_SUB.easy.experiments` is still “Lần chạy”. | **Proved** on the nav label |
| Stories `DaSup` / `HaiTuongTac` | Both exports exist. | **Proved** |
| e2e + design contract | `e2e/health-replay.spec.ts` and `audit/design-contracts/R6.md` on disk. | **Proved** |

No `TWIN` or `SYNTHETIC` value is filled as `BOARD` on these two screens.

---

## Repository law

Allowed tree for this product lane: `web/glassbox/`, `services/glassbox/`, `contracts/glassbox/`, `.cursor/agents/`.

Inspected R6 units and wiring:

- `web/glassbox/src/components/tabs/metrics.tsx`
- `web/glassbox/src/components/tabs/experiments.tsx`
- `web/glassbox/src/components/charts/health-lines.tsx`
- `web/glassbox/src/components/viz.tsx` (`HeatRow`)
- `web/glassbox/src/fixtures/session.ts` (SYNTHETIC health + 1841/1842)
- `web/glassbox/src/lib/data.ts` (`TABS` labels only for this turn)
- stories + `e2e/health-replay.spec.ts` + `playwright.config.ts` + `audit/design-contracts/R6.md`

Workspace has **no git index**, so hardware-tree cleanliness is inferred from unit paths, not a diff. No new UART command, ILA, or LiteScope **core** in these two tabs. No silicon `*_PASS` / `BOARD_PASS` in the R6 unit files.

`data.ts` still contains a `gates` array with `status: "PASS"` and `src: "BOARD"` for Tab 13. That is **not** this turn’s claim and is not a silicon declaration by this reviewer.

---

## Skills the parent said it read

Parent custom instructions for this review did not name skill paths. I therefore do not certify any skill-read claim.

This reviewer followed `.cursor/agents/gb-independent-reviewer.md` (exit gate, repository law, honesty audit, dated file) and the workspace MUST_READ (GlassBox lane only; no encoder RTL).

---

## §19 / §20 coverage

Present: collapse verdict from the series; alert with rank / sat / AUC trails; long-horizon lines vs update count; beating baselines including histogram 0.880; awaiting-signal list (not invented numbers); two recorded interaction ids; before/after table from compare + representation + memory; hidden BEFORE/AFTER heatmaps; stage-replay entry via existing store; links to Học and Sóng.

Incomplete, not blocking this sign-off:

- SPEC §19 chart also lists `max |Wh|` and `M_L1` lines — omitted from `HealthLines`.
- SPEC §20 sample `#500` is omitted on purpose (honesty).
- Weight comparison is a Wh write count, not a delta heatmap.
- Memory comparison is HIT/MISS text, not added / updated / evicted.
- Waveform replay is a jump to Tab 10, not a timeline scrubber.
- 1841 empty compare / missing AFTER pair is unstoried (empty copy exists in the tab).
- `startReplay` is not e2e-clicked and always names #1842.

---

## Blocking findings

None for **TURN_PASS** of these two units.

---

## Residual (does not flip this verdict; still blocks system-level claims)

1. `build-storybook` not run; no `storybook-static`.
2. Each new story is a single fixture state.
3. `contrast-check.mjs` not re-run.
4. Full Playwright suite (R2–R6) not re-run after adding `health-replay.spec.ts` to `testMatch`.
5. First 4/2 Playwright failure has no log.
6. Pass count / 9.7s not stored in `.last-run.json`; tsc stdout not archived.
7. `e2e/shell.spec.ts` (reduced-motion / focus) remains outside `testMatch`.
8. Visible focus and reduced motion not re-asserted on Tab 11 / Tab 12.
9. No digit shortcut reaches Sức khỏe / Replay (`TABS` has Episode before them).
10. Per-KPI `EvidenceBadge` is not universal; AUC sub-copy says `HOST EVAL` over a SYNTHETIC value.
11. Replay CTA does not bind to the selected before/after ids.
12. Weight delta heatmap and waveform scrubber are unspecified-complete for §20.
13. `data.ts` `gates` / Evidence-tab BOARD PASS rows remain (out of unit, still product debt).
14. Frontend not wired to backend (correct for this phase).
15. No git index: hardware-tree cleanliness is inferred from unit paths.

---

## §36 (these two screens only)

Advanced, not product-complete: #2 researcher numbers on the health series and A/B table, #5 provenance at series/compare level, #6 before/after of two recorded interactions, #7 collapse visible and explained, #10 no invented #500 / no invented gates PASS on these tabs, #11 SYNTHETIC not filled as BOARD, #13 no mockup chrome on these two tabs.

Not met for the product: live silicon (#3), remaining tabs, system claims.

---

**TURN_PASS**
