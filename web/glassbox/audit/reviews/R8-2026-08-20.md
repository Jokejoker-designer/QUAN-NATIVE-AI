# Independent review — R8 §26 / §27 / §28 (2026-08-20)

Reviewer: `gb-independent-reviewer`. 2026-08-20.

Scope: SPEC §26 loading / empty / error, §27 responsive (1440 / 1280), §28 accessibility only.

Verdict: **TURN_PASS**.

R7 remains **TURN_PASS**. This review does not reopen it.

Not claimed, and not reached: `FRONTEND_PASS`, `BACKEND_PASS`, `GLASSBOX_SYSTEM_PASS`, silicon `BOARD_PASS`.

---

## Commands (cross-checked; none invented)

| Command | Claim | Independent evidence | Verdict |
|---|---|---|---|
| `web/glassbox` `npx tsc --noEmit` | exit 0, empty stdout, after R8 edits | No log file. Active terminals folder has only an encoder twin sweep, not `tsc`. No contradicting `.tsbuildinfo` error. | PASS — accepted as claimed; not independently replayed |
| `web/glassbox` `npm run build` | exit 0 **after** the 1440 CSS fix; client `.output/public/assets/routes-Dt8XGeV0.js` **618.18 kB**; `styles-CDWeuYw9.css` **34.71 kB** | Files exist. Lengths **618184** B and **34710** B (decimal kB match). Both written **23:09:59**, after `styles.css` / `shell.tsx` **23:09:08**. Stdout itself is not in terminals. | PASS — a Vite/Nitro rebuild landed after the CSS fix |
| First `npx playwright test e2e/states-a11y.spec.ts` | **13 passed / 3 failed** (open-insight count vs hidden; reduced-motion duration string) | No failure report, no `playwright-report/`, no leftover `test-results` traces. Claim is an admitted first miss. | recorded — **not independently proved**; not treated as a later pass |
| After spec+CSS fix: same spec | **16 passed (17.8s)** — 8 tests × 2 viewports | Spec has **8** tests. `playwright.config.ts` has **2** projects (1440 / 1280). `test-results/.last-run.json` `status: "passed"`, `failedTests: []`, written **23:09:27** (after spec **23:08:44** and CSS **23:09:08**). Pass **count and 17.8s are not stored**. | PASS — config math + last-run after the fix; duration not visible |
| Full Playwright suite R2–R7 | not re-run this turn | `testMatch` lists seven specs including this one. last-run does not name which files ran. | recorded, not a unit FAIL |
| `build-storybook` | not run | `web/glassbox/storybook-static` **absent**. | recorded, not a unit FAIL (see Storybook row) |
| `contrast-check.mjs` | not run | No new contrast log this turn. | residual harness; not a unit FAIL |

This reviewer did **not** re-run `tsc`, `build`, Playwright, Storybook, or contrast-check.

---

## Exit gate

| Gate item | Authoritative evidence | Classification | Verdict |
|---|---|---|---|
| `npm run build` exit 0 with output shown | `routes-Dt8XGeV0.js` 618184 B + `styles-CDWeuYw9.css` 34710 B at 23:09:59, after `styles.css` 23:09:08 | Proves a production chunk landed after the 1440 media-query edit; stdout not captured | PASS |
| `tsc --noEmit` exit 0 with output shown | Claim only; empty stdout | Too weak to re-prove; no contrary artifact | PASS |
| Storybook story for every added component | `studio-state.stories.tsx`: `DangTai`, `FpgaChuaKetNoi`, `ChuaChonTuongTac`, `KhongCoSong`, `TraceThieu`, `CaptureTran`, `TimingHong`, `Loi`. `.storybook/main.ts` already globs `../src/**/*.stories.tsx`. `AppErrorComponent` has **no** story (thin route wrapper around `kind="error"`). `build-storybook` was **not** run. Story `onClick` handlers are `() => undefined` (stories only). | Stories **exist on disk** for `StudioState`. Letter of this gate (“story exists”) met. | PASS |
| Playwright covers primary flow of both units | `e2e/states-a11y.spec.ts`: disconnected banner → `Mở session` → Replay + “Không bịa Interaction #500”; 1841 empty wave + toast + no `LiteScope groups`; 1841 evidence `Trace chưa đầy đủ`; `]` cycle to `tab-evidence`; skip link `#gb-main`; 1280 drawer / 1440 rail; health table `512` / `0.5`; reduced-motion duration `< 0.05s`; waveform cursor `ArrowRight` increments `aria-valuenow`; forbidden copy. Config 1440 + 1280. last-run after the spec = passed. | Proves the listed §26/§27/§28 flows. Does **not** open AppError, overflow, timing-invalid, or `no-interaction`. | PASS |
| anti-ui-slop finish gate | Disconnected title is SPEC copy. Buttons go to Replay / Evidence. Empty wave uses the SPEC title and a toast with a recorded outcome. `Mở insight` opens the rail. Design contract `audit/design-contracts/R8.md` exists and records UIZZE **not** browsed. | No inert control on the wired app surfaces. Storybook buttons are inert by construction. | PASS |
| No placeholder / mockup copy (§32.1–§32.8) | Grep of `studio-state.tsx` / `shell.tsx` / `error-component.tsx` + e2e `FORBIDDEN` on overview and waveform at both viewports | Proves absence of the listed strings on those screens | PASS |
| Every displayed metric has a provenance badge (§25) | These units are banners, a drawer, and a11y chrome. Header still shows `INTERACTION.activeSource` (`SYNTHETIC`, `live: false`) as a warn pill. Health table numbers are the existing series (HOST EVAL / recorded). No new BOARD-filled KPI. | No new unbadged scientific claim introduced | PASS |
| Chart types match §8.1 | No new chart type. Health table is an accessible companion to the existing long-horizon lines. | No chart to mismatch | PASS |
| Vietnamese copy §30; forbidden `bộ não AI` / `AI suy nghĩ` / `ý thức` | Absent in the unit files. Tab labels `Bằng chứng`, `Cài đặt`, `Bo mạch` in `data.ts` `TABS`. E2E asserts the three phrases. | Proves forbidden phrases absent; required tab names present | PASS |
| Keyboard, visible focus, reduced motion on both units | Keyboard: `[` / `]` walk `TABS` (includes Evidence). Skip link `href="#gb-main"`. Cursor `role="slider"` + arrows. Focus: global `:focus-visible` 2px cyan; skip link `.gb-sr-only:focus` unhides. **No** e2e assertion of outline width. Reduced motion: `styles.css` sets `animation-duration: 0.01ms !important`; e2e probes `.gbx-active` and requires parsed seconds `< 0.05`. | Keyboard + reduced-motion proved this turn. Focus is CSS + skip-link, not measured. | PASS — residual focus measurement |
| Rendered at 1440 and 1280 | Two Playwright projects; last-run after the spec passed; CSS `@media (min-width: 1440px)` hides `.gb-insight-open` and static-positions `.gb-insight-rail` | Proves tests ran at both viewports, not a visual screenshot audit. Tablet / mobile §27 not in this turn. | PASS |

---

## Honesty audit (parent claims)

| Claim | Evidence | Result |
|---|---|---|
| Default source SYNTHETIC; no fake live BOARD on disconnect | `data.ts` `live: false`, `activeSource: "SYNTHETIC"`. Banner renders because `!INTERACTION.live`. Header pill is warn + `SYNTHETIC`. `Mở session` only `setTab("experiments")`. E2E asserts “Không bịa Interaction #500”. | **Proved** |
| Capture button records intent + toast; no LiteScope / UART / hardware | `requestCaptureNext` sets `captureNextRequested` and toasts “Đã ghi yêu cầu capture cho lần sau” / “Không trang bị LiteScope”. No UART write, no new command. 1841 absence `reason: "CAPTURE_DISABLED"`. E2E: toast visible, `LiteScope groups` count 0. | **Proved** |
| Waveform 1841 empty, not a drawn fake wave | `waveform.tsx` returns `StudioState kind="no-waveform"` when `!result.available`. Viewer/canvas is not mounted on that path. | **Proved** |
| Evidence 1841 partial-trace | `StudioState kind="partial-trace"` when `audit.verdict === "PARTIALLY_TRACEABLE"`. | **Proved** |
| Board timing-invalid **only if** `VIOLATED`; fixture is `MET` | `board.tsx` `{timingStatus === "VIOLATED" ? <StudioState kind="timing-invalid" /> : null}`. `session.ts` `timingStatus: "MET"`. Banner therefore **not** shown on the fixture. | **Proved** |
| Hydrate loading | `routes/index.tsx` (mtime **21:48**, pre-R8) shows `<p role="status">Đang tải studio…</p>` until `useEffect`. It does **not** mount `StudioState kind="loading"`. Loading copy exists; the catalog component is unused on that path. | **Proved as a paragraph**; incomplete vs `StudioState` |
| `AppErrorComponent` | `router.tsx` `defaultErrorComponent: AppErrorComponent` wraps `StudioState kind="error"`. No e2e. | **Proved in code**; path untested |
| Insight drawer below 1440 | `styles.css` `@media (min-width: 1440px)` `.gb-insight-open { display: none }`, rail `position: static; transform: none`. Below that, rail stays `translate-x-full` until `insightOpen`. | **Proved** |
| Skip link `#gb-main`; `[` `]` cycle including Bằng chứng | `shell.tsx` skip + key handler over full `TABS`. E2E: one `]` → Live, then 13× `]` → Evidence. | **Proved** |
| Waveform cursor `role="slider"` + arrows | `viz.tsx` `role="slider"`, `aria-valuenow`, ArrowLeft/Right/Home/End. E2E ArrowRight. | **Proved** |
| Health values as a visible table | Toggle `Hiện bảng số` → `health-value-table` with update count / AUC. Also a `gb-sr-only` table. | **Proved** |
| Vietnamese tab labels Bằng chứng / Cài đặt / Bo mạch | `data.ts` `TABS` labels match. | **Proved** |
| Reduced-motion in `styles.css` | `@media (prefers-reduced-motion: reduce)` forces `0.01ms` animation/transition. E2E after the duration-string fix. | **Proved** |
| First Playwright 13/3 | No artifact | **Unproved** (admitted miss only) |

No session `TWIN` or `SYNTHETIC` value is filled as `BOARD` on these units.

---

## Repository law

Allowed tree for this product lane: `web/glassbox/`, `services/glassbox/`, `contracts/glassbox/`, `.cursor/agents/`.

R8 writes after 22:55 (mtime) sit under `web/glassbox/src/**`, `web/glassbox/e2e/states-a11y.spec.ts`, `playwright.config.ts`, `audit/design-contracts/R8.md`, and build output. `contracts/glassbox` / `services/glassbox` / `.cursor/agents` have **no** writes in that window.

Workspace has **no git index**, so hardware-tree cleanliness is inferred from mtimes, not a diff. Newest `rtl/**` **11:08:24**; `vivado/**` **11:17:10**; `*.bit` **11:16:15**. Those are the same morning artifacts R7 already recorded. They are **hours before** the R8 sources (23:02–23:09). No new UART command, ILA, or LiteScope **core**. Store toast **names** LiteScope only to deny it. No silicon `*_PASS` / `BOARD_PASS` in the R8 unit files.

A concurrent encoder twin sweep is running in another terminal. That is outside this sign-off and is not treated as GlassBox evidence.

---

## Skills the parent said it read

| Path | What the file actually enforces | What this turn shows |
|---|---|---|
| `.agents/skills/anti-ui-slop/SKILL.md` | Browse UIZZE or label missing refs; write a design contract; finish gate | `R8.md` **states UIZZE was not browsed**. Finish-gate letter met on wired surfaces. Catalogue browse **not** done — skill allows continue-from-repo if labeled. |
| `.agents/skills/wcag-accessibility-audit/SKILL.md` | POUR audit WCAG 2.2 AA with pages/screenshots; contrast tools | Keyboard / skip / slider / health table / `lang="vi"` / reduced-motion are in code + e2e. Contrast-check **not** run. No screen-reader pass. Skill was **not** executed as a full audit. |
| `.agents/skills/a7-fpga-gate/SKILL.md` | Toolchain / frozen bits; AI cannot declare `BOARD_PASS`; LiteScope before Native V1 is a hard stop | No FPGA recipe was run for this lane (correct). Capture path refuses to arm hardware. |
| `C:\Users\phant\.agents\skills\find-skills\SKILL.md` | Discover/install skills | No product rule. |
| `MUST_READ_UNBLOCK_H5.md` | Encoder next = ungated DIFF twin; GlassBox is a listed encoder ban; no `BOARD_PASS` | This lane stayed in `web/glassbox`. Encoder next is unchanged. |

**Skill gap (flagged, not treated as a read):** `od-*`, `vercel-*`, and `web-design-guidelines` `SKILL.md` files were **not** found as installable skills on this machine this turn.

This reviewer followed `.cursor/agents/gb-independent-reviewer.md` (exit gate, repository law, honesty audit, dated file) and the workspace MUST_READ (GlassBox lane only; no encoder RTL).

---

## §26 / §27 / §28 coverage

Present and wired: FPGA-disconnected banner with SPEC title/body and `Mở session` / `Dùng Twin`; 1841 no-waveform with SPEC title/body and `Bật capture cho lần sau` (toast only); 1841 partial-trace; capture-overflow **component** if `capture.overflow`; timing-invalid **only** on `VIOLATED`; AppError `kind="error"`; 1440 three-column rail vs 1280 drawer; skip link; `[`/`]` tab cycle including Bằng chứng; waveform slider; visible health table; `lang="vi"`; reduced-motion kill switch; Vietnamese tab names.

Incomplete, not blocking this sign-off:

- Hydrate loading is a `<p>`, not `StudioState kind="loading"`.
- `no-interaction` exists in copy + Storybook only; the app always has a selected fixture id.
- Capture-overflow and timing-invalid are not exercised by the fixture / e2e.
- `AppErrorComponent` has no story and no e2e.
- Visible focus outline is not measured in Playwright.
- Contrast-check and `build-storybook` were not run.
- §27 tablet-landscape / mobile are unspecified this turn.
- First 13/3 Playwright miss has no leftover report.
- Full R2–R7 suite was not re-run (as claimed).

---

## Blocking findings

None for **TURN_PASS** of these units.

---

## Residual (does not flip this verdict; still blocks system-level claims)

- No `FRONTEND_PASS` / `BACKEND_PASS` / `GLASSBOX_SYSTEM_PASS`.
- Contrast-check not run on the accessibility turn.
- Storybook not built; story actions are inert.
- Focus visibility and screen-reader behavior not measured.
- `no-interaction` / overflow / timing-invalid / error route untested in e2e.
- R2–R7 regression not re-run after the CSS/spec fix.
- Encoder work remains ungated DIFF twin, not this UI.

---

## Verdict

**TURN_PASS**

R9 may start. System-level GlassBox claims remain closed.
