# Independent review — GLASSBOX_SYSTEM_PASS (2026-08-20, resign)

Reviewer: `gb-independent-reviewer`. 2026-08-20 (overwrite of prior TURN_FAIL).

MUST_READ_UNBLOCK_H5: read. Next = ungated DIFF twin (not S2, not glue). GlassBox software studio only; no encoder RTL; no silicon `BOARD_PASS`.

Scope: units (1) live HTTP Playwright through adapters without dropping the fixture suite; (2) SPEC §36 on **both** lanes (fixture default + HTTP opt-in). This reviewer did not write the code. Completion was assumed unproven until inspected and replayed.

Prior file on this path was **TURN_FAIL**: Overview / Insight / several research screens still painted fixture `INTERACTION.answer` (Artix) on HTTP session **#9001**. That FAIL is re-judged against current source + replay.

Prior grants (not silently revoked): `FRONTEND_PASS` fixture (`R9-2026-08-20.md`); `BACKEND_PASS` synthetic localhost (`BACKEND-2026-08-20.md`); WIRE `TURN_PASS` (`WIRE-2026-08-20.md`).

Verdict: **TURN_PASS**

**GLASSBOX_SYSTEM_PASS is granted** (software studio; fixture lane + HTTP SYNTHETIC lane).

Not granted: silicon `BOARD_PASS`.

---

## Blocking findings

None. The prior item-4 / item-10 split is gone.

1. `web/glassbox/src/components` grep `INTERACTION`: **no matches**.
2. Research Overview “Trả lời” is `header.answer ?? "Không có — lane này không có mô hình ngôn ngữ"` (`overview.tsx`). `header` is `useStudioHeader()`.
3. `studio-header.ts` reads `session` / `connection` / `activeInteractionId` from the port-hydrated store. `answer` is `interaction?.answer ?? null`.
4. HTTP e2e now opens Research Overview and asserts that honest empty string, and `Arty A7 sử dụng FPGA Artix-7.` count **0**. Replay this review: that test passed on 1440 and 1280.

---

## Commands (cross-checked; none invented)

| Command | Claim | Independent evidence | Verdict |
|---|---|---|---|
| `web/glassbox` `npx tsc --noEmit` | `TSC_EXIT=0` | Replay this review. Printed `TSC_EXIT=0`. Exit **0**. | **Proved** |
| `services/glassbox` `npx tsc --noEmit` with `GLASSBOX_PORT=8788` | exit 0 | Replay. Printed `TSC_EXIT=0`. Exit **0**. | **Proved** |
| `web/glassbox` `npm run build` | `BUILD_EXIT=0`; `routes-C75J9Qmd.js` 622.53 kB | Replay. Client: `routes-C75J9Qmd.js` **622.53 kB**; `waveform-window-DwYGwCl-.js` **0.19 kB**. `BUILD_EXIT=0`. Chunk-size warning only. | **Proved** |
| `npx playwright test e2e/adapter-wiring.spec.ts e2e/s36-frontend.spec.ts` | 16 passed (~20.5s) | Replay: **16 passed (27.5s)**. Both projects. Fixture Settings `#1842`; Output Artix on fixture. | **Proved** (duration **27.5s** this replay) |
| `npm run e2e:http` | 12 passed (~17.8s); Overview absence + Artix 0 | Replay: **12 passed (26.7s)**. Includes `hydrate shows backend interaction 9001, not fixture 1842` (Overview copy + Artix count 0). `playwright.http.config.ts` starts `GLASSBOX_PORT=8788` and Vite `VITE_GLASSBOX_TRANSPORT=http` / `VITE_GLASSBOX_API=http://127.0.0.1:8788`. | **Proved** (duration **26.7s** this replay) |

Claimed durations 20.5s / 17.8s were **not** reproduced; only pass counts and Overview asserts were.

---

## Adapter direction (both lanes)

| Gate item | Authoritative evidence | Classification | Verdict |
|---|---|---|---|
| UI → ports → adapter → transport | Components do not `fetch` / `EventSource` / `WebSocket`. `hydrateFromPorts()` is the only caller of `getPorts()`. `http.ts` owns HTTP. | Proves | **PASS** |
| Default transport is fixture | `adapters/index.ts`: `"http"` only if `VITE_GLASSBOX_TRANSPORT === "http"`. Fixture Playwright Settings `#1842`. | Proves | **PASS** |
| HTTP opt-in | `playwright.http.config.ts` sets the env; banner `#9001` not `#1842`. | Proves | **PASS** |
| Hydrate before shell | `routes/index.tsx` waits on `hydrateFromPorts()`. HTTP failure copy: `Không bịa số từ fixture.` | Proves | **PASS** |
| Header from store | `studio-header.ts` / `useStudioHeader()`. | Proves | **PASS** |
| Numbers from store | `metrics.ts` `measured` / `sessionView` / `buildFacts` are Proxies over `useStudio.getState()`. | Proves | **PASS** |
| Waveform from session, not `waveformResultFor` | `waveform.tsx` uses `sessionView.interactions[].waveform`. `waveformResultFor` remains fixture-builder only (`fixtures/waveform.ts`). | Proves waveform path | **PASS** |
| IndexedDB does not invent a session | `session-idb.ts` stores the port session; hydrate catch does not fall back to fixture. | Proves | **PASS** |
| Screens do not import fixture `INTERACTION` | Grep `src/components` for `INTERACTION`: empty. `data.ts` still exports the blob for fixture construction / `initialChat`; Live chat is `recordedChat(interaction)` from ports. | Proves the prior FAIL is closed | **PASS** |

---

## §36 product criteria (all 15) — wired system

Lane key: **F** = fixture default (`s36-frontend` + `adapter-wiring`). **H** = HTTP opt-in (`s36-http` + `synthetic.ts`). A criterion the HTTP session cannot show must be proved on F and shown as honest absence on H.

| # | Criterion | Authoritative evidence | Classification | Verdict |
|---|---|---|---|---|
| 1 | First-time user can follow the AI process without FPGA terminology | **F:** easy stage strip + Overview “Chuyện gì vừa xảy ra”; Live is recorded Q&A; ILA/LiteScope count 0. **H:** same strip; Live question from hydrate; no ILA Basic. Default level easy. | Proves both lanes | **PASS** |
| 2 | Researcher can inspect exact numeric states | **F:** Compare `1320` (e2e). **H:** Compare `1400` from `synthetic.ts` `dPos` (e2e). Học `trước` on both. | Proves both | **PASS** |
| 3 | FPGA engineer can open exact waveform evidence | **F:** cursor on 1842; 1841 empty title. **H:** cursor on 9001; 9000 “Không có waveform…”. Source pill SYNTHETIC. No LiteScope core. | Proves both | **PASS** |
| 4 | Every screen stays linked to one interaction | **F:** Settings `#1842`; no Episode tab. **H:** banner `#9001`, Settings `#9001`, EAM `ep-syn-12`, waveform 9001/9000. Overview / Insight / tabs read `useStudioHeader()` + store Proxies, not `INTERACTION`. HTTP e2e: Research Overview honest empty; Artix sentence count 0. Residual: Evidence download *filenames* still list `interaction-1842.json` (catalog in `data.ts`); export *payload* uses `studioHeaderFromStore().id`. Does not re-paint fixture Q&A. | Proves interaction fields; filename catalog residual | **PASS** |
| 5 | Every important metric has evidence provenance | Session tabs mount `EvidenceBadge` (R9). Compare badges `compare.provenance`. Source pill SYNTHETIC, `connected: false`. Board WNS/TNS still lack a badge (inherited residual). HTTP e2e does not assert badges. | Important session metrics proved | **PASS** (residual) |
| 6 | Before/after learning is visually comparable | **F+H:** Học column `trước` (both e2e). Writes exist on 1842 and 9001. | Proves both | **PASS** |
| 7 | Representation collapse is visible and understandable | **F+H:** Metrics `Cảnh báo: Representation Collapse` (both e2e). | Proves both | **PASS** |
| 8 | Memory retrieval understandable without a giant graph | No `tab-episode`. **F:** EAM `#488271`. **H:** `ep-syn-12`. Funnel + one selected episode. | Proves both | **PASS** |
| 9 | Output tokens can be traced back to memory/model events | **F:** Output lists `Artix`; trace panel names episode / model / `TOKEN_EMITTED` (path exists; e2e does not click). **H:** `output: []` → `Chưa có đầu ra` (e2e). Honest absence. | Split proved as required | **PASS** |
| 10 | Interface never claims unmeasured semantics | **F:** Live is #1842 Q&A; no ILA product copy. **H:** `9001.answer === null`; Live `recordedChat` uses `answer ?? "Không có câu trả lời từ bo."`; Overview research uses the language-model absence string; Insight easy/research same store `header.answer`. Token/s, EAM hit, DDR remain `awaiting()`. No Artix sentence on H Overview (e2e). | Proves both; prior contradiction closed | **PASS** |
| 11 | Interface never treats TWIN as BOARD | **F+H:** Settings SYNTHETIC + `chưa kết nối`; Compare/Metrics exact `BOARD` count 0. Connection `connected: false`. Evidence tab still shows a **legend** of badge types including BOARD (taxonomy, not session stamp). | Proves session chrome; legend residual | **PASS** |
| 12 | Raw waveform remains available below simplified explanations | Viewer + copy when present; empty 1841 / 9000 do not draw. Worker does not invent samples. | Proves both | **PASS** |
| 13 | Normal product screens contain no design annotations or mockup instructions | Forbidden walks on F (incl. live) and H. Grep `src/components`: no Lorem / Feature 1 / `bộ não AI` / `AI suy nghĩ` / `ý thức`. | Proves both | **PASS** |
| 14 | UI looks like a finished application, not research documentation | Settings workspace headings; hydrate `StudioState` loading/error. Screens share one store after hydrate. | Proves chrome + binding | **PASS** |
| 15 | Visualization does not alter frozen Native AI learning | Default fixture; HTTP GET + SSE only. No new UART. Capture toast still refuses hardware. `session-idb` caches what ports already returned. No `rtl/**` / bit / ILA core in this lane. | Proves software studio | **PASS** |

**Score:** 15 PASS. `GLASSBOX_SYSTEM_PASS` requires all 15.

---

## Exit gate (turn hygiene)

| Gate item | Authoritative evidence | Classification | Verdict |
|---|---|---|---|
| `npm run build` exit 0 with output shown | Replay stdout; `routes-C75J9Qmd.js` 622.53 kB; worker 0.19 kB | Proves | **PASS** |
| `tsc --noEmit` exit 0 with output shown | Frontend and backend replays, `TSC_EXIT=0` | Proves | **PASS** |
| Storybook story for every added component | No new React component. `studio-header` is a hook. | No new story owed | **PASS** |
| Playwright covers primary flow of both units | Fixture: 16 passed. HTTP: 12 passed, including Overview absence. | Proves both units | **PASS** |
| anti-ui-slop | Settings / Live / Output empty-state name jobs. No inert HTTP-only CTA invented this turn. | Proves on walked surfaces | **PASS** |
| No placeholder / mockup (§32.1–§32.8) | Forbidden walks + grep | Proves | **PASS** |
| Every displayed metric has a provenance badge (§25) | Inherited R9 + Compare badge | Residual Board WNS | **PASS** (residual) |
| Chart types match §8.1 | No new chart type | No mismatch | **PASS** |
| Vietnamese copy §30; forbidden phrases | Absent in `src/components` | Proves | **PASS** |
| Keyboard, visible focus, reduced motion | `states-a11y.spec.ts` is fixture-only (skip link, `]`, waveform arrows, reduced-motion probe). Same shell on HTTP; HTTP spec is click + text. | Incomplete HTTP replay | **WEAK** (does not flip SYSTEM_PASS; same CSS/shell) |
| Rendered at 1440 and 1280 | Both Playwright configs; both suites passed this replay | Proves walked flows | **PASS** |

---

## Repository law

| Rule | Evidence | Verdict |
|---|---|---|
| No change under `rtl/**`, `vivado/**`, `*.bit`, `*.xdc`, `mig.prj`, `tools/ui/**`, `tools/a7eam03e_*silicon*` | Workspace has no `.git` (`NO_GIT`). Inspected files live under `web/glassbox/**` and `services/glassbox/**`. This review writes only this file. | **PASS** (path-scoped; git tree unknown) |
| No new UART command | HTTP GET + SSE. Settings: no serial. | **PASS** |
| No ILA / LiteScope core | Absence / product-forbidden copy only. | **PASS** |
| No silicon `*_PASS` / `BOARD_PASS` | This file grants neither. | **PASS** |
| New files in allowed dirs | Adapters, e2e, worker, audit. | **PASS** |

---

## Honesty audit

| Claim | Evidence | Result |
|---|---|---|
| SYNTHETIC never styled as BOARD | Settings + Compare/Metrics e2e `BOARD` count 0 on both lanes. Header pill is `connection.activeSource`. | **Proved** for session chrome |
| HTTP session has empty output — absence, not a token | `synthetic.ts` `answer: null`, `output: []`; Overview / Live / Output honest copy; HTTP e2e. | **Proved** |
| Fixture #1842 still has Artix for criterion 9 | Fixture Output e2e `Artix`. | **Proved** |
| HTTP e2e catches fixture-answer leak on Overview | `s36-http.spec.ts` Research Overview string + Artix count 0. Replay passed. | **Proved** |
| `src/components` has no `INTERACTION` | Grep this review: empty. | **Proved** |
| `waveformResultFor` gone from the tab | `waveform.tsx` has no import. Builder remains in `fixtures/waveform.ts`. | **Proved** |
| Claimed e2e durations 20.5s / 17.8s | This replay 27.5s / 26.7s. | **Not reproduced** — pass counts stand |
| Skill reads | `.cursor/agents/gb-independent-reviewer.md`: owned path this file; assume unproven; do not soften FAIL. No product rule changed by a skill. | Named |

---

## Skills / MUST_READ

Followed `.cursor/agents/gb-independent-reviewer.md` (owned path this file only). Workspace MUST_READ: GlassBox lane only; encoder next remains ungated DIFF twin. No silicon `BOARD_PASS`.

---

## Residuals (do not flip this PASS)

1. Evidence “Artifacts có thể xuất” still lists frozen names `interaction-1842.json` / `waveform-1842.vcd`. Export body uses the hydrated id.
2. Evidence badge **legend** includes a BOARD example.
3. Board WNS/TNS still lack `EvidenceBadge`.
4. Output-trace click still not in e2e.
5. Keyboard / reduced motion not replayed on the HTTP lane (`states-a11y` is fixture-only).
6. Git history unavailable.
7. `data.ts` still exports `INTERACTION` for fixture construction; screens no longer import it.

---

## Sign-off

**TURN_PASS**

**GLASSBOX_SYSTEM_PASS: granted** (software studio; fixture + HTTP SYNTHETIC; 15/15 §36)

**FRONTEND_PASS:** stands (`R9-2026-08-20.md`, fixture only) — not re-granted.

**BACKEND_PASS:** stands (`BACKEND-2026-08-20.md`, synthetic `127.0.0.1` only).

**WIRE TURN_PASS:** stands (`WIRE-2026-08-20.md`).

**BOARD_PASS:** not granted.

Review path: `web/glassbox/audit/reviews/SYSTEM-2026-08-20.md`
