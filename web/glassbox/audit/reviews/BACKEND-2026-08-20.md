# Independent review — BACKEND (2026-08-20)

Reviewer: `gb-independent-reviewer`. 2026-08-20.

Scope: close `services/glassbox` against `BACKEND_PASS` per
`.cursor/agents/gb-backend-implementation.md` and SPEC §35. Inspect whether
`getPorts()` is still fixture-only.

This reviewer did not write the service. Completion was assumed unproven until
replayed.

Verdict: **TURN_PASS**

**BACKEND_PASS is granted** for the synthetic localhost service only
(`127.0.0.1`, source `SYNTHETIC`, `connected: false`).

Not granted (and not re-granted): `FRONTEND_PASS`, `GLASSBOX_SYSTEM_PASS`,
silicon `BOARD_PASS`.

FE↔BE wiring remains forbidden until a later turn that both signs after this
file. `getPorts()` is still fixture-only.

---

## Commands (cross-checked; none invented)

Parent claimed these in `services/glassbox`. This reviewer replayed both.

| Command | Claim | Independent evidence | Verdict |
|---|---|---|---|
| `npx tsc --noEmit` | exit 0 | Replay this review, cwd `services/glassbox`. Exit **0**. Stdout empty. Printed `TSC_EXIT=0`. | **Proved** |
| `npm test` | 5 passed, 0 failed | Replay this review. `node --test --experimental-strip-types test/*.test.ts`. **tests 5, pass 5, fail 0**. Named tests: session contract; HTTP schema round-trip; waveform range + 9000 `PRE_FREEZE_NOT_PERMITTED` + empty weights + episode 204; SSE resume `?fromCursor=` and `Last-Event-ID`; interaction `0` → 404. Duration 556.6 ms. | **Proved** |

---

## BACKEND_PASS checklist (every required item)

| Gate item | Authoritative evidence | Classification | Verdict |
|---|---|---|---|
| Session store with build identity | `store.ts` `createSyntheticPorts().session.getSession()` returns `synthetic.ts` `Session.parse({ sessionId, build, ... })`. `BuildIdentity` includes `bitstreamSha256: null`, `modelVersion`, `learningLawId`, clocks, timing `UNKNOWN`. Test asserts `session.build.bitstreamSha256 === null`. | Proves | **PASS** |
| Interaction query by id | `GET /v1/interactions/:id` → `Interaction.parse` or 404. Test loads `9001`. Test `GET /v1/interactions/0` status 404. | Proves | **PASS** |
| Telemetry SSE + documented reconnect / replay-from-cursor | `ENDPOINTS.md` + `http.ts` `/v1/endpoints` reconnect sentence. `GET /v1/telemetry` writes `id: cursor` + `data:` frames. Resume: `Last-Event-ID` or `?fromCursor=`; next frame is cursor+1. Test: full stream ≥2 frames; both resume paths return `frames.length - 1` starting at `frames[1].cursor`. | Proves | **PASS** |
| Snapshot: hidden vector | `GET /v1/hidden/:id` → `HiddenVector.array().parse`. Test hits `9001`. Store reads `interaction.representation`. | Proves | **PASS** |
| Snapshot: weight tile | Frozen contract has `WeightWrite`, not a separate tile schema. `GET /v1/weights/:id` returns `WeightWrite.array()` from `learning[].writes` (E/Wh address, before, delta, after). Test: `9001` length > 0; `9000` length 0. Catalog lists `/v1/weights/`. | Proves against frozen Zod; SPEC “selected Wh tile” is this write list | **PASS** |
| Snapshot: embedding row | `GET /v1/embeddings/:id` → `EmbeddingRow.array()`. Generator emits rows only for bytes the interaction actually read. Test parses `9001`. | Proves | **PASS** |
| Snapshot: episode header | `GET /v1/episode/:id` → `RetrievalFunnel.parse` or 204 when `retrieval` is null. Test: `9001` parses; `9000` status 204. | Proves | **PASS** |
| WaveformSource with range; missing capture is absence | Port implements `kind: "SYNTHETIC"`, `listCaptures`, `getCapture`, `getRange`. `GET /v1/waveform/:id/range?from=&to=` slices transitions to `[from,to]`. Test: `9001` range 70–90. `9000` `available: false`, reason `PRE_FREEZE_NOT_PERMITTED`. No fabricated traces. | Proves | **PASS** |
| Health series | `GET /v1/health-series` → `HealthSeries.parse`. Points keyed by `updateCount` 0 and 64. Test parses. | Proves | **PASS** |
| Every contract endpoint round-trips frozen Zod | `http.ts` parses `ConnectionState`, `Session`, `InteractionSummary[]`, `Interaction`, `HealthSeries`, `HiddenVector[]`, `EmbeddingRow[]`, `Projection2D`, `WeightWrite[]`, `RetrievalFunnel`, `WaveformResult`, `TelemetryFrame`. Test covers those plus catalog presence of weights/episode. `/v1/health` and `/v1/endpoints` are operational JSON (documented, not contract types). | Contract-bearing routes proved | **PASS** |
| Streaming survives reconnect | Finite recorded list; second GET with cursor/header continues after last id; no invented samples. Test both resume mechanisms. | Proves documented reconnect | **PASS** |
| Bind `127.0.0.1` only | `http.ts` `HOST = "127.0.0.1"`; `server.listen(port, HOST)`. Tests fetch `http://127.0.0.1:${handle.port}`. `GET /v1/health` reports `bind: "127.0.0.1"`. | Proves | **PASS** |
| No board / Vivado / serial / new UART / ILA / LiteScope core | Service: Node HTTP only. No serial open. LiteScope/ILA appear only as `PRE_FREEZE_NOT_PERMITTED` copy. `index.ts` prints “no serial port, no LiteScope, no BOARD claim”. No `rtl/**`, `vivado/**`, `*.bit`, UART command, or ILA core in this lane. Workspace has no `.git` here; law check is path inspection of owned files. | Proves for this service | **PASS** |
| Documented endpoint list | `services/glassbox/ENDPOINTS.md` lists all routes, bind, SYNTHETIC, SSE reconnect. Runtime `GET /v1/endpoints` returns the same list. | Proves | **PASS** |
| `connected` must not imply a live board | `ConnectionState.parse({ connected: false, activeSource: "SYNTHETIC", detail: "... Not BOARD." })`. Test asserts both fields. | Proves | **PASS** |
| Deterministic synthetic, independent of `web/glassbox` fixtures | `synthetic.ts` imports only `@glassbox/contracts`. IDs `9000`/`9001`, session `ses-backend-synthetic-01`. No import of `web/glassbox`. Same function → same bytes. | Proves | **PASS** |
| No frontend React import | Grep of `services/glassbox/src` + `test`: no `react`, no `@/` app imports. `package.json` deps: contracts + zod only. | Proves | **PASS** |
| FE not wired this turn | `web/glassbox/src/adapters/index.ts` `getPorts()` returns `fixturePorts` only. Adapters dir: `fixture.ts`, `index.ts`. No `127.0.0.1:8787` / `/v1/` import under `web/glassbox/src`. | Proves still fixture-only | **PASS** |

---

## Repository law

| Rule | Evidence | Verdict |
|---|---|---|
| No change under `rtl/**`, `vivado/**`, `*.bit`, `*.xdc`, `mig.prj`, `tools/ui/**`, `tools/a7eam03e_*silicon*` | This workspace has no `.git` from the reviewer shell (`fatal: not a git repository`). Inspected backend files live under `services/glassbox/{src,test,ENDPOINTS.md}`. This review writes only `web/glassbox/audit/reviews/BACKEND-2026-08-20.md`. | **PASS** (path-scoped; git tree unknown) |
| No new UART command | HTTP GET/OPTIONS only. | **PASS** |
| No ILA / LiteScope core | Absence reason string only. | **PASS** |
| No silicon `*_PASS` / `BOARD_PASS` | This file grants service `BACKEND_PASS` only. | **PASS** |
| New code in allowed dirs | Service + this audit file. Contracts consumed, not rewritten this review. | **PASS** |

---

## Honesty audit

| Claim | Evidence | Result |
|---|---|---|
| SYNTHETIC never styled as BOARD | Session `bitstreamSha256: null`. Connection `SYNTHETIC` + `connected: false`. Waveform provenance `SYNTHETIC`. No `source: "BOARD"` in service sources. | **Proved** |
| `bitstreamSha256` is null | `synthetic.ts` + both tests. | **Proved** |
| Skill `websocket-security` was read | `http.ts` comment only. No socket: SSE GET, no Upgrade. Not independently witnessed as a skill-file read. | **Comment only** — not a FAIL; no WebSocket exposed |
| Narrow test is not a missing endpoint | Round-trip test actually fetches hidden, embeddings, projection, weights, episode, health-series, waveform, session, connection, interactions. SSE and range are separate tests. | **Proved** |
| Weight tile vs write list | Parent list + agent brief name “weight tile”. Frozen schema is `WeightWrite[]`. Endpoint exists and round-trips. | Mapped, not missing |
| SPEC §35.2 also lists model-state tile | `ModelEvent[]` is empty; no `/v1/model/:id`. Not in the agent required list or this turn’s BACKEND_PASS bullet list. | Residual, not a unit FAIL |

---

## Frontend exit-gate items (backend-only sign-off)

| Gate item | Verdict |
|---|---|
| `npm run build` (frontend) | **N/A** |
| Storybook story per component | **N/A** |
| Playwright primary flow | **N/A** |
| anti-ui-slop / §32 placeholders | **N/A** |
| Provenance badges / chart types / Vietnamese copy | **N/A** |
| Keyboard / 1440 / 1280 | **N/A** |

These do not apply. Wiring before `BACKEND_PASS` would have been a FAIL; it did not happen.

---

## Blocking findings (priority order)

None.

---

## Residuals (do not flip this verdict)

1. SPEC §35.2 “model-state tile” has no dedicated endpoint; `model` is `[]`.
2. `/v1/health` and `/v1/endpoints` are operational JSON, not Zod contract types.
3. Unknown interaction ids on snapshot routes return empty arrays / 204, not 404 (interaction GET is 404).
4. Git history unavailable in this workspace; repository-law proof is path inspection.

---

## Sign-off

**TURN_PASS**

**BACKEND_PASS granted** — synthetic localhost GlassBox service only.

**FRONTEND_PASS** — not granted in this file.

**GLASSBOX_SYSTEM_PASS** — not granted.

**BOARD_PASS** — not granted.
