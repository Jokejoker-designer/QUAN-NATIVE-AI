# Independent review — R2 Dữ liệu vào + Biểu diễn (2026-08-20)

Verdict for these two units: **PASS with residual harness debt**.
Verdict for cadence advance to R3: **FAIL** until the leftover Next e2e specs are rewritten or retired on the record, contrast checker is re-pointed, and Storybook `build-storybook` is run green (packages are installed; this turn did not complete a storybook build).

## Commands

| Command | Result |
| --- | --- |
| `npx tsc --noEmit` | exit 0 |
| `npm run build` | exit 0 (chunk warning) |
| `npx playwright test` (R2 spec, 1440 + 1280) | 8/8 pass |
| Storybook install `@storybook/react-vite@10.5` | succeeded |
| `build-storybook` | not run this turn |

## Honesty that landed

- Input is UTF-8 bytes from `InputEvent`, not invented word IDs stamped BOARD.
- Embedding barcode only for bytes actually read.
- Representation uses recorded hidden vectors, before/after from two stages, `MINH HỌA 2D` badge, no next-token ranking on this tab.
- Shell is client-mounted after hydration so tab clicks work.

## Still blocking GLASSBOX_SYSTEM_PASS

Old Next-oriented e2e files remain on disk (ignored by `testMatch`). Contrast checker not re-run. Remaining SPEC tabs not signed. Frontend not wired to backend (correct).
