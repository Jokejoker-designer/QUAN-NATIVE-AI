# Independent review — R5 Đầu ra + Sóng FPGA (2026-08-20)

Reviewer: `gb-independent-reviewer`. 2026-08-20.

Scope: Tab 9 Đầu ra (§17) and Tab 10 Sóng FPGA (§18) only.

Verdict: **TURN_PASS**.

Not claimed, and not reached: `FRONTEND_PASS`, `BACKEND_PASS`, `GLASSBOX_SYSTEM_PASS`, silicon `BOARD_PASS`.

---

## Commands (cross-checked; none invented)

| Command | Claim | Independent evidence | Verdict |
|---|---|---|---|
| `web/glassbox` `npx tsc --noEmit` | exit 0 | No log file (empty stdout leaves none). No contradicting `tsc` artifact. | PASS — accepted as claimed; not independently replayed |
| `web/glassbox` `npm run build` | exit 0; chunk warning `routes-BgqT1CyF.js` 640.23 kB | `web/glassbox/.output/public/assets/routes-BgqT1CyF.js` length **640235** bytes (= 640.23 kB), written 2026-08-20 22:27 +07 — same minute as `output.tsx` / after `waveform.tsx` 22:25. | PASS |
| `npx playwright test` | 34 passed (4.4m) exit 0 | `playwright.config.ts` `testMatch` = R2 + R3 + R4 + R5. Tests: 4 + 5 + 4 + 4 = 17 × 2 viewports = **34**. `test-results/.last-run.json` status `passed`, `failedTests: []`, written 2026-08-20 22:32 +07 (after spec 22:27). Pass **count and duration are not stored** in last-run. Terminals folder empty this review. | PASS — count matches config; 4.4m not independently visible |
| `build-storybook` | not run | `web/glassbox/storybook-static` **absent**. | recorded, not a unit FAIL (see Storybook row) |
| `contrast-check.mjs` | not re-run | No new contrast log this turn. | residual harness; not a unit FAIL |

---

## Exit gate

| Gate item | Authoritative evidence | Classification | Verdict |
|---|---|---|---|
| `npm run build` exit 0 with output shown | `routes-BgqT1CyF.js` 640235 B, timestamp this evening after the two tab sources | Proves a Vite/Nitro build landed; stdout itself not in terminals | PASS |
| `tsc --noEmit` exit 0 with output shown | Claim only; empty stdout | Too weak to re-prove; no contrary artifact | PASS |
| Storybook story for every added component | `output.stories.tsx` (`TokenArtix`), `waveform.stories.tsx` (`Capture1842`). One happy-path story each. No loading / empty / error / partial / extreme. `build-storybook` was **not** run. | Stories **exist on disk**. Letter of this gate (“story exists”) met. | PASS |
| Playwright covers primary flow of both units | `e2e/output-waveform.spec.ts`: sentence + Artix click → `72%` + SELECTED + `8.223.401`; exact `BOARD` count 0; traceback Episode #488271; waveform SYNTHETIC `role=img`, annotation “Vi phạm ngưỡng phân biệt” → `2.260`, LiteScope denial; forbidden copy on both tabs; key `9` → Đầu ra. Config includes 1440 and 1280. Last run passed. | Proves token + annotation primary flows. Does **not** click export, open 1841 empty paths, or press a key for Sóng FPGA (no digit binding; `TABS[9]` is waveform). | PASS |
| anti-ui-slop finish gate | Headings name the job (`Câu đã ghi`, `Dòng token`, `Ứng viên bước`, `Sự kiện chọn`, `Cài đặt bản ghi`, `Sóng số`). Token buttons, traceback, group checkboxes, annotation buttons, and export all have destinations. Empty panels exist (`Chưa có đầu ra`, `Không có sóng` / 1841 `CAPTURE_DISABLED`). Design contract `audit/design-contracts/R5.md` exists. | Trigger / pre / post / RLE are a `<dl>` of recorded facts plus “xem, không trang bị capture” — **not** an inert Arm control. Residual: canvas cursor is pointer-only; artifact filename still `litescope-1842.vcd`. | PASS |
| No placeholder / mockup copy (§32.1–§32.8) | Grep of `output.tsx` / `waveform.tsx` + e2e `FORBIDDEN` on both tabs at both viewports | Proves absence of the listed strings on the fixture screens | PASS |
| Every displayed metric has a provenance badge (§25) | Tab-level `EvidenceBadge` on `current.provenance` and `capture.provenance` (fixture `SYNTHETIC`, hairline). Source pills use `INTERACTION.activeSource` / `capture.provenance.source` with `live: false` → warn tone, not solid BOARD. Cycle / % / clock are not each badged. | Incomplete vs “every metric is a badge”. No BOARD fill on these units. | PASS — incomplete, not contradicted |
| Chart types match §8.1 | Next-token → ranked horizontal bars (`kind === "PROBABILITY"` → `%`; SCORE title `điểm (không phải %)`). FPGA signals → `WaveformView` from `traces.transitions` (RLE), not `DensityMap` / `Math.sin()`. | Types match. `Math.sin` remains in unused `DensityMap` in `viz.tsx`. | PASS |
| Vietnamese copy §30; forbidden `bộ não AI` / `AI suy nghĩ` / `ý thức` | Absent in both tab files; e2e asserts the three phrases | Proves | PASS |
| Keyboard, visible focus, reduced motion on both units | Keyboard: spec presses `9`; `TABS` index 8 = `output`. Waveform has no 1–9 key (shell only maps `1..9`). Annotation / group / export are native controls. Focus: global `:focus-visible` in `styles.css`; **no** R5 assertion. Reduced motion: global `@media` in `styles.css`; `e2e/shell.spec.ts` exists but is **outside** `testMatch`. Canvas has no local pulse. | Output keyboard proven. Waveform keyboard / focus / reduced-motion **inherited, not verified this turn**. | PASS — residual harness |
| Rendered at 1440 and 1280 | Two Playwright projects; last run passed | Proves tests ran at both viewports, not a visual screenshot audit | PASS |

---

## Honesty audit (parent claims)

| Claim | Evidence | Result |
|---|---|---|
| Decision/output from `OutputEvent`; `%` only when `kind=PROBABILITY` | `output.tsx` reads `sessionView.outputEvents`. Label `%` only if `c.kind === "PROBABILITY"`. Title switches to `điểm (không phải %)` when first candidate is `SCORE`. Fixture Artix row is `PROBABILITY` 0.72 / 0.11 / 0.07 / 0.04 / 0.06. E2E sees `72%`. | **Proved** |
| Waveform from `WaveformCapture` RLE, not `Math.sin()` | `valueAtCycle` walks `trace.transitions` by cycle. `WaveformView` strokes those transitions. Fixture `buildTraces` emits sparse transitions (`rle: true`). `Math.sin` is only in `DensityMap`, which these tabs do not import. | **Proved** on the two units |
| No BOARD / LiteScope hardware claim; SYNTHETIC badges | Pills = `SYNTHETIC`. Copy: “Không có LiteScope trên bit này”. E2E: exact `BOARD` count 0; `BOARD / LiteScope` count 0. `EvidenceBadge` uses hairline for non-BOARD. | **Proved** |
| Export via `downloadArtifact`; toast says SYNTHETIC | `waveform.tsx` calls `downloadArtifact("litescope-1842.vcd")` and toasts `SYNTHETIC · không phải BOARD` on `out.ok`. `export.ts` VCD path requires `sessionView.waveform.available` and writes a provenance header. **Not** e2e-clicked. | **Proved** in source; toast path untested |
| Trigger as recorded fact (no live arm) vs inert control | Header shows `capture.settings.trigger`. Pre/post/RLE are read-only facts. No Single/Auto/Arm. Explicit “không trang bị capture”. Group checkboxes and annotation buttons **do** work. | **Honest recorded fact**, not an inert arming control |
| No rtl / vivado / bit / UART edits | Inspected R5 units live under `web/glassbox/`. Workspace has **no git index**. No UART/ILA/LiteScope **core** in `output.tsx` / `waveform.tsx` / `WaveformView`. | **No contrary evidence** in the unit set; tree-wide hardware edits are not independently diffable |

SPEC §17 sample cycle `8.218.441` is the **first** token’s cycle in the fixture (`8_218_441 + step * 1_240`). Artix is step 4 → **8.223.401**. The screen and e2e follow the `OutputEvent`, not the SPEC example number. That is honest.

Bar **width** still does `amount * 100` for both kinds (`kind === "PROBABILITY" ? c.amount : c.amount`). All current candidates are `PROBABILITY`, so the label is not a lie. A future SCORE fixture would get a misleading bar. Residual, not a contradiction today.

---

## Repository law

Allowed tree for this product lane: `web/glassbox/`, `services/glassbox/`, `contracts/glassbox/`, `.cursor/agents/`.

Inspected R5 units and wiring:

- `web/glassbox/src/components/tabs/output.tsx`
- `web/glassbox/src/components/tabs/waveform.tsx`
- `web/glassbox/src/components/viz.tsx` (`WaveformView`, `valueAtCycle`)
- `web/glassbox/src/fixtures/session.ts` / `waveform.ts` (SYNTHETIC; 1841 empty output + `CAPTURE_DISABLED`)
- stories + `e2e/output-waveform.spec.ts` + `playwright.config.ts` + `audit/design-contracts/R5.md`

No silicon `*_PASS` / `BOARD_PASS` in these files. FE still fixture-only (correct). Old Next e2e files remain on disk and stay ignored by `testMatch`.

`a7-fpga-gate` hard stop (LiteScope / ILA before Native V1 freeze) applies to the **encoder silicon lane**. Showing a SYNTHETIC `WaveformSource` and refusing live capture is the allowed GlassBox path. This review does not grant any board claim.

Artifact catalog name `litescope-1842.vcd` is a leftover noun. The toast and header deny BOARD. Residual naming, not a hardware edit.

---

## Skills the parent said it read

| Path | What it should have changed | What I see |
|---|---|---|
| `.agents/skills/anti-ui-slop/SKILL.md` | Design contract before code; finish gate (no filler, no inert control) | `audit/design-contracts/R5.md` present. Tab copy is specific. Trigger is a fact list, not a dead Arm button. |
| `.agents/skills/a7-fpga-gate/SKILL.md` | No UART/LiteScope core; no silicon PASS from UI | No hardware in these units; no silicon PASS. Filename `litescope-1842.vcd` is catalog-only. |
| `~/.agents/skills/find-skills/SKILL.md` | Discover skills | Process only; no product rule to check. |
| `~/.cursor/skills-cursor/create-subagent` | Agent spawn, not product UI | Process only. |

I read the first two skill files for this review. I cannot prove the parent read them before writing the tabs.

---

## §17 / §18 coverage

Present: recorded sentence `Arty A7 sử dụng FPGA Artix-7.`; clickable token strip; Artix candidates as probabilities; SELECTED + cycle + interaction id; traceback to episode / model stage / `TOKEN_EMITTED` marker; jumps to Bộ nhớ / Mô hình / Sóng; five capture groups with visibility toggles; digital canvas from RLE; mandatory annotation kinds with Vietnamese labels; cursor inspector via `valueAtCycle`; SYNTHETIC export through `downloadArtifact`; 1841 empty branches in fixture + UI.

Incomplete, not blocking this sign-off:

- SPEC §18 live trigger presets and arming controls are **correctly omitted** (recorded `USER_SENT_QUESTION` only).
- Subsampling is in the capture object but not shown on the settings row.
- 1841 empty output / no-wave paths are unstoried and not the e2e path.
- Export toast not asserted.
- Waveform has no digit shortcut; canvas click is mouse-only.
- Insight rail is hidden on waveform (`hideRail`); `WaveInsight.cursorNs` is unused on that tab.

---

## Blocking findings

None for **TURN_PASS** of these two units.

---

## Residual (does not flip this verdict; still blocks system-level claims)

1. `build-storybook` not run; no `storybook-static`. Stories-on-disk satisfy the turn gate; they do **not** prove axe-on-build.
2. Each new story is a single fixture state. 1841 empty output / `CAPTURE_DISABLED` are unstoried.
3. `contrast-check.mjs` not re-run.
4. `e2e/shell.spec.ts` (reduced-motion / focus) remains outside `testMatch`.
5. Visible focus and reduced motion not re-asserted on Tab 9 / Tab 10.
6. Per-KPI `EvidenceBadge` is not universal (cycle, %, MHz).
7. Playwright pass count / 4.4m duration not stored in `.last-run.json`; terminals empty here.
8. VCD catalog name `litescope-1842.vcd` still says LiteScope; toast corrects it.
9. SCORE bar width still scales `amount * 100` (label path is kind-safe; width is not).
10. `DensityMap` in `viz.tsx` still uses `Math.sin`; unused by these units.
11. Frontend not wired to backend (correct for this phase).
12. No git index: hardware-tree cleanliness is inferred from unit paths, not a diff.

---

## §36 (these two screens only)

Advanced, not product-complete: #2 researcher numbers on Artix candidates and inspector values, #5 provenance at event/capture level, #9 traceback to episode/model/waveform marker, #11–12 SYNTHETIC waveform under the explanation, #13 no mockup chrome on these two tabs.

Not met for the product: live silicon waveform (#3), remaining tabs, Episode invented list, system claims.

---

**TURN_PASS**
