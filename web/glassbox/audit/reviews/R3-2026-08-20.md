# Independent review — R3 So sánh + Học (2026-08-20)

Reviewer: `gb-independent-reviewer`. 2026-08-20.

Scope: Tab 5 So sánh (§13) and Tab 6 Học (§14) only.

Verdict: **TURN_PASS**.

Not claimed, and not reached: `FRONTEND_PASS`, `BACKEND_PASS`, `GLASSBOX_SYSTEM_PASS`, silicon `BOARD_PASS`.

---

## Commands (cross-checked; none invented)

| Command | Claim | Independent evidence | Verdict |
|---|---|---|---|
| `web/glassbox` `npx tsc --noEmit` | exit 0, empty stdout | No log file (empty stdout leaves none). No contradicting `tsc` artifact. | PASS — accepted as claimed; not independently replayed |
| `web/glassbox` `npm run build` | exit 0; vite client+ssr+nitro; chunk warning `routes-CGXgYBzV.js` 631.56 kB | `.output/nitro.json` dated 2026-08-20T14:53:40Z. `web/glassbox/.output/public/assets/routes-CGXgYBzV.js` length **631563** bytes (= 631.56 kB), written 2026-08-20 21:53:39 +07. | PASS |
| First `npx playwright test` | 16 passed, 2 failed (strict `getByText('trước')`) | Spec now uses `getByRole("columnheader", { name: "trước" })`. Hint text `trước · Δ · sau` would collide with a bare `getByText('trước')`. Failure mode is plausible; first-run stdout not archived. | PASS — first-run count not independently visible; cause matches the file |
| After locator + unique keys | 18 passed (41.7s), 1440 and 1280 | `playwright.config.ts`: `testMatch` = R2 + R3 specs; 2 projects. 4 + 5 tests × 2 viewports = **18**. `test-results/.last-run.json` status `passed`, `failedTests: []`, written 2026-08-20 21:55:13 +07. | PASS |
| `build-storybook` | not run | `web/glassbox/storybook-static` **absent**. | recorded, not a unit FAIL (see Storybook row) |
| `contrast-check.mjs` | not re-run | No new contrast log this turn. | residual harness; not a unit FAIL |

---

## Exit gate

| Gate item | Authoritative evidence | Classification | Verdict |
|---|---|---|---|
| `npm run build` exit 0 with output shown | Nitro + `routes-CGXgYBzV.js` 631563 B, timestamps this evening | Proves a Vite/Nitro build landed; stdout itself not in terminals | PASS |
| `tsc --noEmit` exit 0 with output shown | Claim only; empty stdout | Too weak to re-prove; no contrary artifact | PASS |
| Storybook story for every added component | `compare.stories.tsx` (`CanHocThem`), `learning.stories.tsx` (`Ghi286`). Preview wraps styles only; tabs use Zustand so they render without a provider. | Stories **exist on disk**. One happy-path story each. No loading / empty / error / partial / extreme. `build-storybook` was **not** required by the letter of this gate (“story exists”). | PASS |
| Playwright covers primary flow of both units | `e2e/compare-learning.spec.ts`: A/P/N + “Cần học thêm”; research cosine/`không dùng`; write log + clickable timeline; forbidden copy; keys 5/6. Config includes both viewports. Last run passed. | Proves primary flows. Does **not** prove visible focus or reduced motion on these tabs. | PASS |
| anti-ui-slop finish gate | Headings name the job (`Ba câu đang so sánh`, `Nhật ký ghi`, `Cần học thêm`). Timeline buttons and `Xem quyết định so sánh` / insight `Xem chỗ vừa đổi` navigate. Empty panels exist when compare/learn events are missing. No Lorem / Feature 1 / mockup labels in these files. | No `audit/design-contracts/R3.md` (skill requires a contract before code). Residual process debt. Screens themselves are product-specific. | PASS |
| No placeholder / mockup copy (§32.1–32.8) | Source grep + e2e `FORBIDDEN` list on both tabs | Proves absence of the listed strings on the fixture screens | PASS |
| Every displayed metric has a provenance badge (§25) | Tab-level `EvidenceBadge` on `compare.provenance` and `learn.provenance` (fixture `SYNTHETIC`, hairline / non-authoritative). Some KPIs put `provenance.source` in `sub`. Research `M_cos` sub is only `ĐO / EVAL`. Several learning research KPIs have no source line. `Kpi` is not `EvidenceBadge`. | Incomplete vs “every metric is a badge”. Event-level SYNTHETIC is present; no BOARD fill. | PASS — incomplete, not contradicted |
| Chart types match §8.1 | Distance → `DistBars` horizontal bars. Margin → `MarginGauge` centered scale. Weight change → 32×32 Δ heatmap from `writes` where `target === "Wh"`. Update distribution → `HistChart` histogram. | Types match. Gauge center is labeled `Ngưỡng` but geometry is margin=0 on a hard-coded ±4000 scale, not `marginThreshold` 512. | PASS — geometry note recorded |
| Vietnamese copy §30; forbidden `bộ não AI` / `AI suy nghĩ` / `ý thức` | Absent in both tab files; e2e asserts the three phrases | Proves | PASS |
| Keyboard, visible focus, reduced motion on both units | Keyboard: spec presses `5` then `6`; `TABS` index matches. Focus: global `:focus-visible` in `styles.css`; **no** R3 assertion. Reduced motion: global `@media` in `styles.css`; `e2e/shell.spec.ts` exists but is **outside** `testMatch`. These tabs have no local pulse. | Keyboard proven. Focus / reduced-motion **inherited, not verified this turn**. | PASS — residual harness |
| Rendered at 1440 and 1280 | Two Playwright projects; last run passed | Proves tests ran at both viewports, not a visual screenshot audit | PASS |

---

## Honesty audit (parent claims)

| Claim | Evidence | Result |
|---|---|---|
| Decision uses `compare.violated`, not `margin > 0` | `compare.tsx` `needUpdate = compare?.violated ?? false`. Fixture `violated: true`, `marginL1` `derivedInt(3490, …)`. Screen copy “Cần học thêm” while gauge shows `+3490`. Insight uses the same flag. | **Proved** |
| Cosine labeled ĐO / EVAL | Research KPI `M_cos` sub `"ĐO / EVAL"`; insight appends `ĐO/EVAL`. Fixture `marginCosine` is DERIVED from hidden vectors. | **Proved** |
| dH shown as không dùng / null, not zero-filled | Fixture `dH: null`. UI `measured.dH ? … : "không dùng"`. | **Proved** |
| Learning shows writes only (trước / Δ / sau); no gradient column | Table headers: đích, địa chỉ, trước, Δ, sau. Words “gradient” appear only as **denial** copy, not a column. | **Proved** |
| Wh 32×32 Δ heatmap from real writes | Grid 32×32; cells set from `writes` with `target === "Wh"` and `address` → (r,c). Fixture emits 286 writes (141 up / 145 down), including Wh addresses in `0..1023`. | **Proved** |
| Histogram + clickable timeline | `hist` from write signs; `HistChart`. Timeline buttons set `picked` and show `Đang chọn: …`. E2E clicks `Vi phạm ngưỡng`. | **Proved** |
| Provenance SYNTHETIC; no BOARD styling | Event badges use `EvidenceBadge` + `SYNTHETIC` (`authoritative: false`). `INTERACTION.live === false` → `Pill` tone `warn`, text `SYNTHETIC`. E2E `BOARD` exact count 0. | **Proved** |
| No `rtl/**`, vivado, bit, UART, LiteScope | Claimed unit files are under `web/glassbox/`. Workspace has no git index to diff. No UART/ILA/LiteScope strings in these units. | **No contrary evidence**; hardware work was not invented as missing |

---

## Repository law

Allowed tree for this product lane: `web/glassbox/`, `services/glassbox/`, `contracts/glassbox/`, `.cursor/agents/`.

Inspected R3 units and wiring:

- `web/glassbox/src/components/tabs/compare.tsx` (new)
- `web/glassbox/src/components/tabs/learning.tsx` (slimmed)
- `web/glassbox/src/components/shell.tsx` (`compare` icon + view)
- `web/glassbox/src/lib/data.ts` (`TabId` / tab row)
- `web/glassbox/src/components/insight.tsx` (`CompareInsight` uses `violated`)
- `web/glassbox/src/components/tabs/overview.tsx` (`PHASE_TAB.COMPARE → "compare"`)
- stories + `e2e/compare-learning.spec.ts` + `playwright.config.ts`

No silicon `*_PASS` / `BOARD_PASS` in these files. FE still fixture-only (correct). Old Next e2e files remain on disk and stay ignored by `testMatch`.

`a7-fpga-gate` hard stop L3 (GlassBox / ILA / LiteScope before Native V1 freeze) applies to the **encoder silicon lane**. This review does not treat GlassBox UI work as a silicon bypass, and does not grant any board claim.

---

## Skills the parent said it read

| Path | What it should have changed | What I see |
|---|---|---|
| `.agents/skills/anti-ui-slop/SKILL.md` | Design contract before code; finish gate (no filler, no inert control) | No `audit/design-contracts/R3.md`. Tab copy is specific. |
| `.agents/skills/a7-fpga-gate/SKILL.md` | No UART/LiteScope; cosine not TRAIN; no host gradient-as-evidence; no silicon PASS from UI | Cosine EVAL; no gradient column; no silicon PASS in these units. |
| `.agents/skills/wcag-accessibility-audit/SKILL.md` | Keyboard, focus, contrast, names | Keyboard tested. Heatmap has `role="img"` + `aria-label`. Contrast not re-run. Focus / reduced-motion not asserted on these units. |

I read those three files for this review. I cannot prove the parent read them before writing the tabs.

---

## §13 / §14 coverage

Present: A/P/N texts from fixture (SPEC sample sentences); distance bars 1320 / 4810; margin marker +3490; decision card on `violated`; research `d_pos` / `d_neg` / `M_L1` / `M_cos` / `dH`; learning summary from recorded counts (9216 / 286 / 141 / 145 / 0 / 12.4 ms); Δ-only Wh matrix; E rows that actually wrote; histogram; clickable timeline; causal sentence; before/Δ/after log.

Incomplete, not blocking this sign-off:

- Research list in §13 also names **norm** — not on the tab.
- Optional Before / After heatmap modes — not shipped (Δ only is the SPEC default).
- Gauge “Ngưỡng” is visual center (margin 0), not law threshold 512.
- `MarginGauge` always paints the number `text-ok` and prefixes `+`. Here the value is +3490, so the glyph matches SPEC; green must not be read as “law satisfied” while `violated` is true.

---

## Blocking findings

None for **TURN_PASS** of these two units.

---

## Residual (does not flip this verdict; still blocks system-level claims)

1. `build-storybook` not run; no `storybook-static`. Stories-on-disk satisfy the turn gate; they do **not** prove axe-on-build.
2. Each new story is a single fixture state. Empty compare / empty learn branches are unstoried.
3. `contrast-check.mjs` not re-run.
4. `e2e/shell.spec.ts` (reduced-motion) and leftover Next e2e files remain on disk; current `testMatch` ignores them.
5. Visible focus and reduced motion not re-asserted on Tab 5 / Tab 6.
6. No R3 design contract.
7. Per-KPI `EvidenceBadge` is not universal.
8. Frontend not wired to backend (correct for this phase).

---

## §36 (these two screens only)

Advanced, not product-complete: #2 researcher numbers, #5 provenance at event level, #6 before/Δ/after writes, #10–11 no unmeasured gradient / no TWIN-as-BOARD, #13 no mockup chrome on these tabs.

Not met for the product: waveform (#3, #12), memory graph (#8), token traceback (#9), remaining tabs.

---

**TURN_PASS**
