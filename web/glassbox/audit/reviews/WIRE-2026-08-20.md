# Independent review — WIRE (2026-08-20)

Reviewer: `gb-independent-reviewer`. 2026-08-20.

Scope: adapter-only FE↔BE wiring after both lane PASS files. Units: (1) `BACKEND_PASS` already signed in `BACKEND-2026-08-20.md` — not re-litigated except contradiction check; (2) `getPorts()` / hydrate / HTTP opt-in.

This reviewer did not write the adapters. Completion was assumed unproven until inspected and replayed.

Verdict: **TURN_PASS**

**GLASSBOX_SYSTEM_PASS is not granted.**

Not re-granted here: `FRONTEND_PASS` (stands from `R9-2026-08-20.md`, fixture only), `BACKEND_PASS` (stands from `BACKEND-2026-08-20.md`, synthetic `127.0.0.1` only), silicon `BOARD_PASS`.

---

## Commands (cross-checked; none invented)

| Command | Claim | Independent evidence | Verdict |
|---|---|---|---|
| `services/glassbox` `npx tsc --noEmit` | exit 0 | Replay this review. Exit **0**. Printed `TSC_EXIT=0`. | **Proved** |
| `services/glassbox` `npm test` | 6 passed, 0 failed | Replay this review. `node --test --experimental-strip-types test/*.test.ts`. **tests 6, pass 6, fail 0**. Duration 677.7 ms. Sixth test is `HTTP client shaped like GlassBoxPorts…` in `http-ports-client.test.ts` (additive; does not contradict `BACKEND-2026-08-20.md`). | **Proved** |
| `web/glassbox` `npx tsc --noEmit` | exit 0 | Replay this review. Exit **0**. Printed `TSC_EXIT=0`. | **Proved** |
| `web/glassbox` `npm run build` | exit 0; `routes-CHQmSrAp.js` 619.26 kB | Replay this review. Vite client: `.output/public/assets/routes-CHQmSrAp.js` **619.26 kB**. `BUILD_EXIT=0`. Chunk-size warning only. | **Proved** |
| `npx playwright test e2e/adapter-wiring.spec.ts` | 2 passed (7.7s) at 1440 and 1280 | Replay this review. `ok 1 [desktop-1440]`; `ok 2 [laptop-1280]`. **2 passed (7.9s)**. Asserts Settings `SYNTHETIC`, `chưa kết nối`, `#1842`. | **Proved** (duration 7.9s this replay) |
| `npx playwright test e2e/s36-frontend.spec.ts` | 14 passed (prior); adapter later fixed | Not re-run this review. `FRONTEND_PASS` already signed on that spec. Adapter spec is the unit under review. | Inherited, not replayed |

---

## BACKEND_PASS contradiction check

| Check | Evidence | Classification | Verdict |
|---|---|---|---|
| Prior sign-off still true | `BACKEND-2026-08-20.md` grants synthetic localhost only. Service tests still parse `SYNTHETIC`, `connected: false`, `127.0.0.1`. Extra client-shape test does not claim a board. | No contradiction | **Stand** |
| Prior “FE not wired” line is stale | That file recorded `getPorts()` fixture-only. This later turn is the allowed wiring. Stale relative to now; not a defect in the backend grant. | Historical | **N/A** |

---

## Wiring unit (this turn)

| Gate item | Authoritative evidence | Classification | Verdict |
|---|---|---|---|
| Dependency direction UI → ports → adapter → transport | Components import `@/lib/store`, `@/lib/metrics`, `@/lib/data`. They do not import `@/adapters/http` or `createHttpPorts`. `hydrateFromPorts()` is the only caller of `getPorts()`. `http.ts` is the only file with `fetch` / `EventSource`. | Proves | **PASS** |
| `getPorts()` default is fixture, not always HTTP | `adapters/index.ts`: `transportName()` is `"http"` only if `VITE_GLASSBOX_TRANSPORT === "http"`; else `fixturePorts`. Playwright default (no env) shows `#1842`. | Proves | **PASS** |
| HTTP selected only via env | `createHttpPorts(import.meta.env.VITE_GLASSBOX_API ?? "http://127.0.0.1:8787")` inside the `http` branch. `vite-env.d.ts` types the flag. | Proves | **PASS** |
| Hydrate before shell paint | `routes/index.tsx`: `ready` stays false until `hydrateFromPorts()` resolves; loading `StudioState`; `StudioShell` only after ready. | Proves | **PASS** |
| HTTP failure does not silently fall back to fixture | `.catch` sets `error`; if `transportName() === "http"` the copy is `Không đọc được dịch vụ SYNTHETIC … Không bịa số từ fixture.` Error `StudioState` replaces the shell. | Proves | **PASS** |
| `metrics.ts` reads port-hydrated store | `snapshot()` / `activeInteraction()` use `useStudio.getState()` (`session`, `embeddingRows`, `projection`, `activeInteractionId`). Hydrate writes those four plus `connection`. | Proves for measured/sessionView | **PASS** |
| Settings reads connection from store | `settings.tsx` `useStudio()` → `connection.activeSource`, `connection.connected`, `activeInteractionId`. No fetch. | Proves | **PASS** |
| Components do not import `fetch` / `EventSource` / `WebSocket` | Grep `src/components`: no matches. Grep `src` for those APIs: only `adapters/http.ts`. | Proves | **PASS** |
| No raw WebSocket | `http.ts` comment + implementation: GET + SSE (`EventSource` or fetch text). No `WebSocket`, no Upgrade. | Proves | **PASS** |
| Default Playwright still fixture `#1842` | `adapter-wiring.spec.ts` `#1842` exact on `#gb-main`. Replay passed at both viewports. Fixture session id is 1842; backend synthetic ids are 9000/9001 — this run did not hit HTTP. | Proves | **PASS** |
| No silicon claim | Connection copy: SYNTHETIC, `connected: false`, “không phải bằng chứng silicon”. Settings: “SYNTHETIC không được tô như BOARD”. No `BOARD_PASS` in adapter sources. | Proves | **PASS** |

FAIL triggers named by the parent (components fetch / `getPorts` always HTTP / hydrate skipped) are **absent**.

---

## Exit gate (turn hygiene)

| Gate item | Authoritative evidence | Classification | Verdict |
|---|---|---|---|
| `npm run build` exit 0 with output shown | Replay stdout above; `routes-CHQmSrAp.js` 619.26 kB | Proves | **PASS** |
| `tsc --noEmit` exit 0 with output shown | Frontend and backend replays, `TSC_EXIT=0` | Proves | **PASS** |
| Storybook story for every added component | No new React component this turn. Adapters are not UI. `settings.stories.tsx` already on disk from FRONTEND_PASS. | No new story owed | **PASS** |
| Playwright covers primary flow of both units | Wiring: adapter spec (hydrate → Settings SYNTHETIC / `#1842`) at 1440 and 1280. Backend unit: service tests, not a browser. | Proves wiring + service; not live HTTP UI | **PASS** |
| anti-ui-slop | Settings headings name jobs (`Hồ sơ dự án`, `Hiển thị`, `Nguồn dữ liệu`). Hydrate error names the failed SYNTHETIC service. No interchangeable filler. | Proves on this turn’s surfaces | **PASS** |
| No placeholder / mockup (§32.1–§32.8) | Grep `src` components + adapters: no Lorem Ipsum, `Feature 1`, `bộ não AI`, `AI suy nghĩ`, `ý thức`. Adapter e2e: LiteScope capture groups count 0. | Proves | **PASS** |
| Every displayed metric has a provenance badge (§25) | No new metric widget. Settings shows source/connection, not a new chart number. Session metrics inherit FRONTEND_PASS. | Inherited | **PASS** |
| Chart types match §8.1 | No new chart type. | No mismatch | **PASS** |
| Vietnamese copy §30; forbidden phrases | Error string and Settings copy are Vietnamese product language. Forbidden phrases absent. | Proves | **PASS** |
| Keyboard, visible focus, reduced motion | Not re-measured this turn. Inherited from `states-a11y` / R9. Adapter spec is click + text. | Inherited, not re-measured | **PASS** (residual) |
| Rendered at 1440 and 1280 | Playwright projects `desktop-1440` and `laptop-1280`; both ok this replay | Proves adapter flow | **PASS** |

---

## Repository law

| Rule | Evidence | Verdict |
|---|---|---|
| No change under `rtl/**`, `vivado/**`, `*.bit`, `*.xdc`, `mig.prj`, `tools/ui/**`, `tools/a7eam03e_*silicon*` | Workspace has no `.git` (`fatal: not a git repository`). Inspected wiring files live under `web/glassbox/src/adapters/**`, `routes/index.tsx`, `lib/metrics.ts`, `components/tabs/settings.tsx`, `e2e/adapter-wiring.spec.ts`. This review writes only this file. Unrelated encoder twin terminals were already running and are outside this sign-off. | **PASS** (path-scoped; git tree unknown) |
| No new UART command | HTTP GET + SSE only in `http.ts`. Settings: “Studio không mở cổng serial.” | **PASS** |
| No ILA / LiteScope core | Adapter e2e count 0 for “LiteScope capture groups”. No core added. | **PASS** |
| No silicon `*_PASS` / `BOARD_PASS` | This file grants neither. | **PASS** |
| New code in allowed dirs | Adapters, route hydrate, e2e, this audit file. | **PASS** |

---

## Honesty audit

| Claim | Evidence | Result |
|---|---|---|
| Default transport remains fixture | `getPorts()` + Playwright `#1842` | **Proved** |
| HTTP adapter unused unless env set | Branch on `VITE_GLASSBOX_TRANSPORT`. No Playwright with that env. | **Proved**; live HTTP UI untested |
| Shell `INTERACTION` in `data.ts` is still fixture-derived | `data.ts` `INTERACTION` from `FIXTURE_SESSION.interactions[0]`. Overview / Live / shell still import it. | **Proved residual** |
| SYNTHETIC not styled as BOARD | Settings `activeSource` + “chưa kết nối”; fixture connection `connected: false` | **Proved** |
| Skill `websocket-security` | Comment in `http.ts` only; no Upgrade. Not a witnessed skill-file read. | Comment only — not a FAIL |
| `sessionView` after hydrate | Proxy reads store; hydrate writes session/embeddings/projection. Default fixture session matches `#1842`. HTTP session bytes are not what Playwright saw. | Fixture path proved; HTTP path unproved in browser |

---

## Blocking findings (priority order)

None.

---

## Residuals (do not flip this verdict; still block `GLASSBOX_SYSTEM_PASS`)

1. Default transport is still fixture.
2. No Playwright of the live HTTP UI (`VITE_GLASSBOX_TRANSPORT=http` against `127.0.0.1:8787`).
3. Shell `INTERACTION` in `data.ts` remains fixture-derived; several tabs still read it instead of the store.
4. HTTP adapter is unused unless the env flag is set.
5. Keyboard / reduced motion not re-measured this turn.
6. `s36-frontend.spec.ts` not replayed this review.
7. Git history unavailable; repository-law proof is path inspection.

---

## Sign-off

**TURN_PASS**

**GLASSBOX_SYSTEM_PASS** — not granted.

**FRONTEND_PASS** — stands (fixture); not re-granted here.

**BACKEND_PASS** — stands (synthetic localhost); not re-granted here.

**BOARD_PASS** — not granted.

Review path: `web/glassbox/audit/reviews/WIRE-2026-08-20.md`
