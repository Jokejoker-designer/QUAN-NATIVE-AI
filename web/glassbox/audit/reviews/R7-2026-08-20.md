# Independent review — R7 Bằng chứng + Giải thích (2026-08-20)

Reviewer: `gb-independent-reviewer`. 2026-08-20.

Scope: Tab 13 Bằng chứng (§21) and global Giải thích (§22) only.

Verdict: **TURN_PASS**.

R6 remains **TURN_PASS**. This review does not reopen it.

Not claimed, and not reached: `FRONTEND_PASS`, `BACKEND_PASS`, `GLASSBOX_SYSTEM_PASS`, silicon `BOARD_PASS`.

---

## Commands (cross-checked; none invented)

| Command | Claim | Independent evidence | Verdict |
|---|---|---|---|
| `web/glassbox` `npx tsc --noEmit` | exit 0, empty stdout | No log file. Terminals folder has no `tsc --noEmit` capture. No contradicting `tsc` artifact. | PASS — accepted as claimed; not independently replayed |
| `web/glassbox` `npm run build` | exit 0; client chunk `.output/public/assets/routes-kFTNLQ75.js` 643.76 kB | File exists. Length **643764** bytes (= 643.76 kB decimal). Written **2026-08-20 22:54:47 +07**, after `evidence.tsx` **22:54:30**. Stdout itself is not in terminals. | PASS — a Vite/Nitro rebuild landed after the evidence edit |
| `npx playwright test e2e/evidence-explain.spec.ts` | **8 passed (21.3s)** — 4 tests × 2 viewports | `playwright.config.ts` `testMatch` includes `evidence-explain.spec.ts`. Spec has **4** tests × **2** projects = **8**. `test-results/.last-run.json` status `passed`, `failedTests: []`, written **22:55:31** (after spec **22:53:46** and config **22:53:49**). Pass **count and 21.3s are not stored**. | PASS — config math + last-run after the spec; duration not visible |
| Full Playwright suite R2–R6 | not re-run this turn | `testMatch` is now 6 specs. Only last-run exists; no suite log. | recorded, not a unit FAIL |
| `build-storybook` | not run | `web/glassbox/storybook-static` **absent**. | recorded, not a unit FAIL (see Storybook row) |
| `contrast-check.mjs` | not run | No new contrast log this turn. | residual harness; not a unit FAIL |

---

## Exit gate

| Gate item | Authoritative evidence | Classification | Verdict |
|---|---|---|---|
| `npm run build` exit 0 with output shown | `routes-kFTNLQ75.js` 643764 B, timestamp after `evidence.tsx` | Proves a build landed after that edit; stdout not captured | PASS |
| `tsc --noEmit` exit 0 with output shown | Claim only; empty stdout | Too weak to re-prove; no contrary artifact | PASS |
| Storybook story for every added component | `evidence.stories.tsx` (`DayDuTruyVet`). `explain.stories.tsx` (`RankDaSup`, `GradientKhongCo`). `.storybook/main.ts` globs `../src/**/*.stories.tsx`. One happy-path Evidence story; Explain covers rank + gradient only. No 1841 / empty / error. `build-storybook` was **not** run. | Stories **exist on disk**. Letter of this gate (“story exists”) met. | PASS |
| Playwright covers primary flow of both units | `e2e/evidence-explain.spec.ts`: twin warning exact sentence; `FULLY_TRACEABLE`; table `M_L1` / `DERIVED` / `Gradient estimate`; law id; no `litescope` / `BOARD PASS`; select `1841` → `PARTIALLY_TRACEABLE` + `Thiếu`; metrics-tab Giải thích dialog `AUC = 0.500` and not `0.742`; forbidden-copy on both units. Config includes 1440 and 1280. Last run after the spec = passed. | Proves evidence table + live-AUC explain. Does **not** open Giải thích on Tab 13 (gradient), press a key, or assert rank `1/32`. Test title says “collapsed rank” but asserts AUC. | PASS |
| anti-ui-slop finish gate | Headings name the job (`MÔ HÌNH / TWIN MODE`, `Bảng nguồn của tương tác này`, `Truy vết đủ 8 câu?`, `Artifacts có thể xuất`, `Nhận dạng bản dựng`). Select changes `#1842` / `#1841`. `Tải` calls `downloadArtifact`. Giải thích toggles a `role="dialog"`. Design contract `audit/design-contracts/R7.md` exists and records UIZZE **not** browsed. Nav chrome still says `Evidence` (English) while SPEC names the tab `Bằng chứng`. | No inert control on these units. Residual English nav label. | PASS |
| No placeholder / mockup copy (§32.1–§32.8) | Grep of `evidence.tsx` / `explain.tsx` / `explain-rules.ts` + e2e `FORBIDDEN` on both units at both viewports | Proves absence of the listed strings on the fixture screens | PASS |
| Every displayed metric has a provenance badge (§25) | Table rows use `EvidenceBadge` from `Interaction.evidence` (SYNTHETIC / DERIVED / TWIN). Source pill = `INTERACTION.activeSource` (`SYNTHETIC`, `live: false` → warn). Legend shows a **BOARD** badge as a type example (filled, `authoritative: true`). Build-identity WNS / clock / law ids are **not** each badged. | Incomplete vs “every metric is a badge”. Session **rows** are not BOARD-filled. | PASS — incomplete, not contradicted |
| Chart types match §8.1 | These units are a provenance table + dialog, not a §8.1 chart. No new chart type introduced. | No chart to mismatch | PASS |
| Vietnamese copy §30; forbidden `bộ não AI` / `AI suy nghĩ` / `ý thức` | Absent in the unit files; e2e asserts the three phrases. Nav label remains English `Evidence`. | Proves forbidden phrases absent. Nav label incomplete vs SPEC tab name. | PASS |
| Keyboard, visible focus, reduced motion on both units | Keyboard: shell maps `1..9` only. `TABS` index 13 = Evidence (Episode sits at 10). No digit reaches this tab. Giải thích is a `<button>`. Focus: global `:focus-visible` in `styles.css`; **no** R7 assertion. Reduced motion: global `@media` in `styles.css`; `e2e/shell.spec.ts` remains outside `testMatch`. | Inherited, not verified this turn on Tab 13 / the dialog. | PASS — residual harness |
| Rendered at 1440 and 1280 | Two Playwright projects; last run after the spec passed | Proves tests ran at both viewports, not a visual screenshot audit | PASS |

---

## Honesty audit (parent claims)

| Claim | Evidence | Result |
|---|---|---|
| Fixture source is SYNTHETIC | `data.ts` `FIXTURE_CONNECTION.activeSource: "SYNTHETIC"`, `live: false`. Session evidence rows in `session.ts` are SYNTHETIC / DERIVED / TWIN only. | **Proved** |
| Legend may show BOARD as a type example; current-session rows must not be BOARD-filled | `evidence.tsx` legend constructs `provenance={{ source: "BOARD", ... }}` for the catalogue only. `#1842` rows: `d_pos`/`d_neg`/`Waveform`/`Weight delta`/`AUC` SYNTHETIC; `M_L1`/`M_cos` DERIVED; `Gradient estimate` TWIN. `#1841` one row `d_pos` SYNTHETIC. `EvidenceBadge` fills only `authoritative` sources (BOARD). TWIN/SYNTHETIC/DERIVED are hairline. | **Proved** |
| Twin warning exact sentence | Copy: `Dữ liệu hiện tại không phải silicon evidence.` shown when `!INTERACTION.live`. E2E asserts the sentence and `MÔ HÌNH / TWIN MODE`. | **Proved** |
| 1842 `FULLY_TRACEABLE`, 1841 `PARTIALLY_TRACEABLE` | `auditTraceability` with all eight keys true vs five missing. Verdicts computed, not hardcoded strings. | **Proved** |
| Giải thích uses live last health point (AUC 0.500 / rank 1/32), not stale glossary (AUC 0.742 / rank 19) | `explain-rules.ts` reads `sessionView.health.points.at(-1)`. Last series row is AUC `0.5`, rank `1`. Titles: `AUC = ${a.toFixed(3)}` → `0.500`; rank `${n}/${dim}` → `1/32`. Does **not** import `data.ts` `glossary`. Glossary still exists with `AUC = 0.742` and `Effective rank = 19/32` but is unused by `Explain`. E2E proves the AUC title and the absence of `0.742` in the dialog. Rank `1/32` is **not** e2e-asserted. | **Proved** for AUC; rank by code only |
| Artifact renamed `waveform-1842.vcd`; waveform download updated | `data.ts` `artifacts` name `waveform-1842.vcd`. `waveform.tsx` calls `downloadArtifact("waveform-1842.vcd")`. E2E: `litescope` count 0 on Tab 13. Stale `data.ts` `evidenceRows` still says `LiteScope 256K` / BOARD — **not imported** by `evidence.tsx`. | **Proved** on the units |
| Stories + e2e + design contract | Files on disk. Contract acceptance matches the warning / FULLY / PARTIAL / live AUC-or-rank rules. | **Proved** |

No session-row `TWIN` or `SYNTHETIC` value is filled as `BOARD`. The legend BOARD chip is a catalogue example, not a row of this session.

---

## Repository law

Allowed tree for this product lane: `web/glassbox/`, `services/glassbox/`, `contracts/glassbox/`, `.cursor/agents/`.

Inspected R7 units and wiring:

- `web/glassbox/src/components/tabs/evidence.tsx`
- `web/glassbox/src/components/explain.tsx`
- `web/glassbox/src/lib/explain-rules.ts`
- `web/glassbox/src/components/insight.tsx` (`EvInsight` + rail `Explain`)
- `web/glassbox/src/fixtures/session.ts` (evidence + traceability)
- `web/glassbox/src/lib/data.ts` (`artifacts` rename; `TABS` / `INTERACTION` only for this turn)
- `web/glassbox/src/components/tabs/waveform.tsx` (download name)
- stories + `e2e/evidence-explain.spec.ts` + `playwright.config.ts` + `audit/design-contracts/R7.md`

Workspace has **no git index**, so hardware-tree cleanliness is inferred from mtimes, not a diff. Latest `rtl/**` write **2026-08-20 11:08**; `vivado/**` **11:17**; `*.bit` **11:16**. R7 sources and the client chunk are **22:52–22:55**. No new UART command, ILA, or LiteScope **core** in these units. No silicon `*_PASS` / `BOARD_PASS` in the R7 unit files.

`data.ts` still contains `gates[]` with `status: "PASS"` and `src: "BOARD"`, plus `evidenceRows` BOARD / LiteScope lines. Those arrays are **not** rendered by Tab 13 this turn.

---

## Skills the parent said it read

| Path | What the file actually enforces | What this turn shows |
|---|---|---|
| `.agents/skills/anti-ui-slop/SKILL.md` | Browse UIZZE or label missing refs; write a design contract; finish gate (no inert control, no filler, no vague CTA) | Contract exists and **states UIZZE was not browsed**. Finish-gate letter met on the two units. Catalogue browse **not** done — skill allows continue-from-repo if labeled. |
| `.agents/skills/a7-fpga-gate/SKILL.md` | Toolchain / numeric gates / frozen bit SHA; AI cannot declare `BOARD_PASS`; LiteScope before Native V1 is a hard stop | No FPGA recipe was run (correct). Units avoid LiteScope **filename** and silicon pass copy. Skill does not authorize encoder RTL. |
| `.agents/skills/wcag-accessibility-audit/SKILL.md` | POUR audit at WCAG 2.2 AA with pages/screenshots | Contrast-check **not** run. No R7 keyboard / focus / reduced-motion assertion. Skill was not executed as an audit. |
| `C:\Users\phant\.agents\skills\find-skills\SKILL.md` | Discover/install skills via `npx skills` | No product rule. Does not change R7 UI. |
| `C:\Users\phant\.cursor\skills-cursor\create-subagent\SKILL.md` | How to author Cursor subagents (`disable-model-invocation: true`) | No product rule. Does not change R7 UI. |
| `MUST_READ_UNBLOCK_H5.md` | Encoder next = ungated DIFF twin; no S2 clamp; no glue; GlassBox is a listed encoder ban; no `BOARD_PASS` | This lane stayed in `web/glassbox`. Encoder next is unchanged: ungated DIFF twin, not S2, not glue. |

This reviewer followed `.cursor/agents/gb-independent-reviewer.md` (exit gate, repository law, honesty audit, dated file) and the workspace MUST_READ (GlassBox lane only; no encoder RTL).

---

## §21 / §22 coverage

Present: twin-mode warning with the frozen sentence; source legend (BOARD as type only); per-interaction evidence table from recorded metadata; eight-question audit with FULLY / PARTIAL; artifacts including `waveform-1842.vcd`; build identity with null bitstream / source SHA shown as `chưa có`; rail + panel `Giải thích` driven by `explainTopic` live numbers (AUC 0.500, rank 1/32, sat 100%, gradient absent).

Incomplete, not blocking this sign-off:

- SPEC §21 sample table stamps d1 / Waveform / Weight delta as BOARD and Waveform as LiteScope. The fixture **correctly refuses** those stamps.
- SPEC §21 badge list omits SYNTHETIC; the screen adds it (honest for this fixture).
- Nav label is still English `Evidence`.
- Giải thích e2e covers AUC on Tab 11, not rank on Tab 4 and not gradient on Tab 13.
- `data.ts` `glossary` / `gates` / BOARD `evidenceRows` remain as unused debt.
- WNS body still says “bitstream này” while bitstream SHA is null.
- `ForwardInsight` still prints `measured.effectiveRank` (ANCHOR vector **19**), which is a different quantity from the health-series last point (**1**). `Explain` rank uses the series.

---

## Blocking findings

None for **TURN_PASS** of these two units.

---

## Residual (does not flip this verdict; still blocks system-level claims)

1. `build-storybook` not run; no `storybook-static`.
2. Evidence story is a single fixture state (1842). 1841 PARTIAL is unstoried.
3. `contrast-check.mjs` not run. WCAG skill was not executed as an audit.
4. Full Playwright suite (R2–R6) not re-run after adding `evidence-explain.spec.ts` to `testMatch`.
5. Pass count / 21.3s not stored in `.last-run.json`; tsc stdout not archived.
6. `e2e/shell.spec.ts` (reduced-motion / focus) remains outside `testMatch`.
7. Visible focus and reduced motion not re-asserted on Tab 13 / the dialog.
8. No digit shortcut reaches Bằng chứng (`TABS` has Episode before it).
9. Nav chrome label is `Evidence`, not `Bằng chứng`.
10. Per-field `EvidenceBadge` is not on build-identity numbers.
11. Stale `data.ts` `gates` / BOARD `evidenceRows` / `glossary` (0.742 / 19/32) still exist.
12. Explain e2e title overclaims “rank” while asserting AUC only.
13. UIZZE catalogue not browsed (labeled in the contract).
14. Frontend not wired to backend (correct for this phase).
15. No git index: hardware-tree cleanliness is inferred from mtimes.

---

## §36 (these two screens only)

Advanced, not product-complete: #2 researcher can read recorded sources and the 8-question audit, #5 provenance on table rows, #10 no invented LiteScope / no BOARD-filled session rows, #11 TWIN hairline not BOARD fill, #13 no mockup chrome on these two units.

Not met for the product: live silicon (#3), remaining tabs, system claims.

---

**TURN_PASS**
