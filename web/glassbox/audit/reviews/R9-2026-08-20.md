# Independent review — R9 SPEC §36 FRONTEND (re-audit, 2026-08-20)

Reviewer: `gb-independent-reviewer`. 2026-08-20.

Scope: all 15 items of `NATIVE_AI_GLASSBOX_UXUI_SPEC.md` §36 against the fixture frontend, after the TURN_FAIL blockers.

Prior R9 file on this path was **TURN_FAIL** / **FRONTEND_PASS not granted**. This file overwrites that sign-off.

Verdict: **TURN_PASS**

**FRONTEND_PASS is granted** for the fixture-backed frontend only.

Not granted: `BACKEND_PASS`, `GLASSBOX_SYSTEM_PASS`, silicon `BOARD_PASS`.

Wiring remains forbidden until `BACKEND_PASS` is separately signed.

---

## Commands (cross-checked; none invented)

| Command | Claim | Independent evidence | Verdict |
|---|---|---|---|
| `web/glassbox` `npx tsc --noEmit` | exit 0 | This reviewer replayed it. Exit **0**, empty stdout. | **Proved** |
| `web/glassbox` `npm run build` | exit 0; `routes-BN59SDyO.js` **612.45 kB** | File exists. Length **612455** B (= 612.46 kB decimal). Written **2026-08-20 23:24**, same minute as `data.ts` / `overview.tsx` / `s36-frontend.spec.ts`. Stdout itself is not in terminals. | Proves a rebuild after the FAIL fixes; stdout not shown |
| `npx playwright test e2e/s36-frontend.spec.ts` | **14 passed (20.7s)** | Spec now has **7** tests. Config has **2** projects. 7 × 2 = 14. `test-results/.last-run.json` `status: "passed"`, `failedTests: []`, written **23:25** (after the spec **23:24**). Pass **count and 20.7s are not stored**. | Config math + last-run after the spec; duration not visible |
| Full R2–R7 suite | not re-run | No suite log. | recorded; not required to flip this FRONTEND_PASS |
| `services/glassbox` tests | not claimed this re-audit | Frontend `getPorts()` still returns `fixturePorts` only. | Not `BACKEND_PASS` |

Contrast from the earlier R9 pass (`73` / `0` on `src/styles.css`) was not claimed as new this re-audit. Styles were not part of the FAIL fix.

---

## Exit gate (turn hygiene)

| Gate item | Authoritative evidence | Classification | Verdict |
|---|---|---|---|
| `npm run build` exit 0 with output shown | `routes-BN59SDyO.js` 612455 B at 23:24 | Rebuild landed; stdout missing | PASS (artifact) |
| `tsc --noEmit` exit 0 with output shown | Replay this review, exit 0 | Proves | PASS |
| Storybook story for every added component | No new component this re-audit. `settings.stories.tsx` still on disk. | No new story owed | PASS |
| Playwright covers primary flow of both units | `s36-frontend.spec.ts` now opens Live and walks overview / compare / metrics / waveform / learning / eam / output / settings. 1440 + 1280. | Proves the §36 surfaces named in the spec | PASS |
| anti-ui-slop | Settings and Live headings name jobs. Live CTAs go to Học. No ILA filler dialogue. | Proves on the FAIL surfaces | PASS |
| No placeholder / mockup (§32.1–§32.8) | Grep of `src/components`: no ILA Basic / LiteScope-as-product / Lorem / Feature 1 / bộ não AI. e2e FORBIDDEN now includes `live`. | Proves | PASS |
| Every displayed metric has a provenance badge (§25) | Session tabs badge the contract object. Overview now badges `latencyMs` and `weightsChanged` (easy) and `latencyMs` (research). Header pill is SYNTHETIC. Board WNS still uses report copy + Explain, not a badge. | Important session metrics proved; board timing residual | PASS |
| Chart types match §8.1 | No new chart type. | No mismatch | PASS |
| Vietnamese copy §30; forbidden phrases | Absent in `src`. | Proves | PASS |
| Keyboard, focus, reduced motion | Inherited R8 CSS + `states-a11y`. Not re-run this re-audit. s36 runs at both viewports. | Inherited, not re-measured | PASS (residual) |
| Rendered at 1440 and 1280 | Two Playwright projects; last-run after the 7-test spec | Proves s36 at both viewports | PASS |

---

## §36 product criteria (all 15)

| # | Criterion | Authoritative evidence | Classification | Verdict |
|---|---|---|---|---|
| 1 | First-time user can follow the AI process without knowing FPGA terminology | Easy stage strip uses Nhận câu / Hiểu / So sánh / Học / Nhớ / Suy luận / Trả lời. Overview “Chuyện gì vừa xảy ra” is Vietnamese and names missing LM instead of inventing one. Live easy: recorded #1842 Q&A only; copy says the board has no language model. ILA / LiteScope product-capture turns **removed** (e2e count 0). Board/Artix names are the SPEC subject, not debug jargon. Default level is easy. | Prior contradiction closed | **PASS** |
| 2 | Researcher can inspect exact numeric states | Research So sánh shows `d_pos` 1320 (e2e). Học table trước / Δ / sau. Sức khỏe collapse + series. | Proves | **PASS** |
| 3 | FPGA engineer can open exact waveform evidence | Sóng FPGA viewer; 1842 cursor visible; 1841 SPEC empty title (e2e). Labeled SYNTHETIC. | Proves fixture waveform | **PASS** |
| 4 | Every screen stays linked to one interaction | Shell locks `INTERACTION.id`. No Episode tab (`tab-episode` count 0). Evidence/waveform selectors stay inside the session list. | Proves | **PASS** |
| 5 | Every important metric has evidence provenance | Input / forward / compare / learning / eam / model / output / waveform / metrics / experiments / evidence mount `EvidenceBadge`. Overview now mounts badges on measured latency and `weightsChanged`. Awaiting slots stay “—”. Source pill SYNTHETIC, `live: false`. **Honesty:** research KPI row badges latency only; `weightsChanged` badge is easy-only. Board WNS/TNS still lack a badge (report sentence + Explain). Unused `data.ts` BOARD rows are not imported. | Important session metrics proved | **PASS** |
| 6 | Before/after learning is visually comparable | Học `trước` column (e2e). Biểu diễn Trước / Sau over recorded stages. | Proves | **PASS** |
| 7 | Representation collapse is visible and understandable | Metrics `Cảnh báo: Representation Collapse` (e2e). Verdict copy explains loss of distinction. | Proves | **PASS** |
| 8 | Memory retrieval understandable without an unreadable giant graph | No Episode tab. EAM: recorded funnel + one selected episode; cue map is a single circle; DDR occupancy withheld. | Proves | **PASS** |
| 9 | Output tokens can be traced back to memory/model events | Đầu ra lists `outputEvents` (`Artix` e2e). Trace panel names episode / model stage / TOKEN_EMITTED and jumps to eam / model / waveform. e2e still does not click the trace button. | Path exists | **PASS** |
| 10 | Interface never claims unmeasured semantics | `initialChat` is `INTERACTION.user` / `INTERACTION.answer` (`Board hiện tại dùng chip gì?` / `Arty A7 sử dụng FPGA Artix-7.`). Meta is `latencyMs` (DERIVED from stage ms) · `tokenCount` (SYNTHETIC 7) · `SYNTHETIC`. `sendChat` records a question only. e2e: no ILA Basic, no “LiteScope là lớp capture”, no Episode #488271 on Live. | Prior contradiction closed | **PASS** |
| 11 | Interface never treats TWIN as BOARD | `activeSource: "SYNTHETIC"`, `connected: false`. Settings SYNTHETIC + “chưa kết nối” (e2e). Evidence twin warning. Session rows not BOARD-filled. Compare/metrics e2e: exact `BOARD` count 0. | Proves | **PASS** |
| 12 | Raw waveform remains available below simplified explanations | Waveform tab: copy + viewer when present; 1841 does not draw. | Proves | **PASS** |
| 13 | Normal product screens contain no design annotations or mockup instructions | FORBIDDEN walk now includes `live`. Grep clean for ILA/LiteScope-as-product on components. | Proves | **PASS** |
| 14 | UI looks like a finished application, not research documentation | Settings is workspace-only. Hydrate uses `StudioState kind="loading"`. Live is a recorded session, not an ILA demo. Residual unused BOARD arrays in `data.ts` are not on screen. | Proves on screens | **PASS** |
| 15 | Visualization does not alter frozen Native AI learning | `getPorts()` is fixture-only. No UART / LiteScope core. Teacher/Freeze/Replay are local UI. Capture toast still refuses hardware. | Proves | **PASS** |

---

## Honesty audit (claimed FAIL fixes)

| Claim | Evidence | Result |
|---|---|---|
| `initialChat` is only recorded #1842 Q&A; ILA/LiteScope turns removed | `data.ts` two messages from `INTERACTION.user` / `answer`. Grep of `src/components`: no ILA Basic / LiteScope-as-product. | **Proved** |
| Meta is latency / tokenCount / SYNTHETIC from the fixture | `INTERACTION_RECORD.latencyMs` is `derivedFloat(totalLatencyMs())`; `tokenCount` is `synthInt(7)`; third meta token is `INTERACTION.activeSource` (`SYNTHETIC`). | **Proved** |
| Overview easy/research measured KPIs mount EvidenceBadge on latency and weightsChanged | Easy: both. Research: **latency only**. `weightsChanged` is not a research KPI; Δw in the research story panel has no badge. | **Partial** — not a unit FAIL |
| `s36` opens Live and asserts no ILA / LiteScope product-capture / Episode #488271; forbidden walk includes live | Spec lines 27–34 and tabs list includes `live`. | **Proved** |
| 14 passed (7 × 2) | last-run after the 7-test spec | Accepted as config math; count not stored |
| Unused BOARD arrays not imported | `gates` / `glossary` / `compareBefore` / `episodes` / `evidenceRows` / `easyStory` unused by screens. `artifacts` and `initialChat` are used. `viz.tsx` still imports unused `hidden` / `memCells`. | Residual confirmed |
| Frontend unwired | `adapters/index.ts` → `fixturePorts` | **Proved** |

---

## Repository law

R9 re-audit sources sit under `web/glassbox/src/**`, `web/glassbox/e2e/s36-frontend.spec.ts`, and this review file. No new UART command, ILA core, or LiteScope core. No silicon `*_PASS` in these files.

This reviewer wrote **only** this file.

---

## Skills / MUST_READ

Followed `.cursor/agents/gb-independent-reviewer.md` and workspace MUST_READ (GlassBox lane only; no encoder RTL; no silicon `BOARD_PASS`). Encoder next remains ungated DIFF twin.

---

## Blocking findings

None.

The three prior FAIL items are closed:

1. Live transcript is the recorded #1842 pair with fixture meta.
2. Overview measured latency / write-count carry `EvidenceBadge` (easy).
3. s36 opens Live and forbids the ILA / LiteScope product sentence.

---

## Residual (does not flip FRONTEND_PASS; still blocks system wiring)

- Unused BOARD-stamped arrays remain in `data.ts`.
- Board-tab WNS/TNS have no `EvidenceBadge`.
- Research overview does not badge `weightsChanged`.
- Output-trace click is not in e2e.
- Easy-mode s36 still accepts the nav label `Tổng quan` as a substitute headline.
- R2–R7 Playwright suite was not re-run.
- Frontend is not wired to `services/glassbox`.
- **`BACKEND_PASS` is not granted.**
- **`GLASSBOX_SYSTEM_PASS` is not granted.**

---

## Verdict

**TURN_PASS**

**FRONTEND_PASS: granted** (fixture frontend only; evidence in the §36 table above)

**BACKEND_PASS: not granted**

**GLASSBOX_SYSTEM_PASS: not granted**

Review path: `web/glassbox/audit/reviews/R9-2026-08-20.md`
