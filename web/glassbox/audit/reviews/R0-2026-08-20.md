# R0 review — 2026-08-20

Reviewer: `gb-independent-reviewer`. Units under review: app shell (§6) and
design system (§7), plus the frozen contract and the fixture/adapter layer.

Verdict: **TURN_FAIL**. R0 may not be declared complete and R1 may not start.

## Gate verdicts

| Gate item | Evidence | Verdict |
|---|---|---|
| `npm run build` exit 0 | Turbopack compiled in 4.2s, TypeScript 2.8s, routes `ƒ /` and `○ /_not-found` | PASS |
| `tsc --noEmit` exit 0 | run in `web/glassbox`, exit 0 | PASS |
| Contract schema tests | `node --test contracts/glassbox/test/contract.test.ts` — 11 pass, 0 fail | PASS |
| Fixture conforms to contract | `Session.parse()` executes during page-data collection; the build would fail otherwise | PASS |
| Storybook story per new component | no `.storybook` directory, no `*.stories.tsx` exists | **FAIL** |
| Playwright test for the turn's primary flow | no `e2e/` directory, no `playwright.config.ts` | **FAIL** |
| No inert control | nav renders only implemented tabs; process-strip stages without a target render as text, not links | PASS |
| No placeholder or mockup copy in app screens | `Lorem ipsum`, `Feature 1`, `bộ não AI`, `AI suy nghĩ`, `ý thức` all absent from rendered `body.innerText` | PASS |
| Every metric has a provenance badge | `Metric` and `MetricValue` render `EvidenceBadge` from the wrapped contract type; an unwrapped number is not expressible | PASS |
| Chart types match §8.1 | no chart exists yet | N/A this turn |
| Rendered at 1440px | `window.innerWidth` 1440, layout renders | PASS |
| Rendered at 1280px | `window.innerWidth` 1280, `scrollWidth` 1280, no horizontal overflow | PASS |
| Keyboard navigation verified | see finding R0-1 | **FAIL** |
| Reduced motion verified | media query present in `globals.css`, not exercised against a moving element | **UNVERIFIED** |
| Accessibility audit filed | `audit/a11y/` is empty; no keyboard or screen-reader pass was performed | **FAIL** |

## Repository law audit

No file under `rtl/**`, `vivado/**`, `tools/ui/**`, `tools/a7eam03e_*silicon*`
was created or modified. No `*.bit`, `*.xdc` or `mig.prj` touched. No UART
command added. No ILA or LiteScope core exists. No `*_PASS` claim about
silicon appears in any artifact.

New paths are confined to `web/glassbox/`, `contracts/glassbox/`,
`.cursor/agents/`, plus the empty `services/` directory reserved for the
backend lane. **PASS.**

One deviation to record: `create-next-app` initialised a git repository inside
`web/glassbox` despite `--no-git`. The repository root itself is not under
version control, so this nested repository is the only VCS in the tree. It is
inside an owned directory and was not requested; it is left in place because it
permits per-turn commits, and it is recorded here rather than hidden.

## Blocking findings

**R0-1 — keyboard navigation is effectively absent on the locked view.**
Measured at 1280px on `/?i=1842`: one focusable element in the entire document.
The process strip is navigation per §6.2, but with no tab implemented every
stage falls through to a non-focusable `span`. A keyboard user can reach only
"Chọn tương tác khác". This resolves itself as R1 lands the first two tabs, but
it must be re-measured then rather than assumed. Owner: `gb-ux-product`.

**R0-2 — no Storybook.** The gate requires a story per component before a turn
passes. Eight components shipped this turn with none. Owner:
`gb-storybook-component-qa`.

**R0-3 — no Playwright.** No test covers the picker flow or the interaction
lock of §6.3, which is the one invariant most likely to regress silently.
Owner: `gb-playwright-e2e`.

**R0-4 — no accessibility pass.** Contrast ratios in `globals.css` are stated
in comments but were not measured with a tool, and no screen-reader pass was
run. The comment values must be treated as unverified until measured. Owner:
`gb-accessibility`.

## Honesty check

Every command reported in the turn summary was executed and its output
observed. Two claims in the design-token comments are **not** yet evidence:
the stated contrast ratios (14.6:1, 7.4:1, 4.6:1) were computed by hand, not
measured by a tool, and are pending R0-4.

No value on screen claims `BOARD`. The connection state reads
`activeSource: SYNTHETIC` and the shell says so in Vietnamese. No `TWIN` or
`SYNTHETIC` value is styled with the solid treatment reserved for `BOARD`.

## Required before R0 can pass

R0-2, R0-3 and R0-4 closed; R0-1 re-measured after the first tabs land.
