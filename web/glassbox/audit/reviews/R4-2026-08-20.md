# Independent review — R4 Bộ nhớ + Mô hình (2026-08-20)

Reviewer: `gb-independent-reviewer`. 2026-08-20.

Scope: Tab 7 Bộ nhớ (§15) and Tab 8 Mô hình (§16) only.

Verdict: **TURN_PASS**.

Not claimed, and not reached: `FRONTEND_PASS`, `BACKEND_PASS`, `GLASSBOX_SYSTEM_PASS`, silicon `BOARD_PASS`.

---

## Commands (cross-checked; none invented)

| Command | Claim | Independent evidence | Verdict |
|---|---|---|---|
| `web/glassbox` `npx tsc --noEmit` | exit 0 | No log file (empty stdout leaves none). No contradicting `tsc` artifact. | PASS — accepted as claimed; not independently replayed |
| `web/glassbox` `npm run build` | exit 0; chunk warning `routes-Cafx_KIB.js` 635.13 kB | `.output/nitro.json` date `2026-08-20T15:08:00.580Z`. `web/glassbox/.output/public/assets/routes-Cafx_KIB.js` length **635138** bytes (= 635.14 kB), written 2026-08-20 22:07:59 +07 — after `eam.tsx` 22:04:52 and `model.tsx` 22:07:41. | PASS |
| First / second Playwright | failed twice on strict locators (`Phễu` / `800.000` / `HIT`) | Spec now uses `getByRole("heading", { name: "Phễu truy hồi" })`, `getByText("800.000", { exact: true })`, `getByText("HIT", { exact: true })`. Bare `getByText("Phễu")` would collide with other copy. Failure mode is plausible; first-run stdout not archived. | PASS — first-run count not independently visible; cause matches the file |
| After exact/heading locators | `npx playwright test` → 26 passed (34.7s), 1440+1280 | `playwright.config.ts` `testMatch` = R2 + R3 + R4. Tests: 4 + 5 + 4 = 13 × 2 viewports = **26**. `test-results/.last-run.json` status `passed`, `failedTests: []`, written 2026-08-20 22:10:55 +07 (after spec 22:10:19). Pass **count is not stored** in last-run. | PASS |
| `build-storybook` | not run | `web/glassbox/storybook-static` **absent**. | recorded, not a unit FAIL (see Storybook row) |
| `contrast-check.mjs` | not re-run | No new contrast log this turn. | residual harness; not a unit FAIL |

---

## Exit gate

| Gate item | Authoritative evidence | Classification | Verdict |
|---|---|---|---|
| `npm run build` exit 0 with output shown | Nitro + `routes-Cafx_KIB.js` 635138 B, timestamps this evening | Proves a Vite/Nitro build landed; stdout itself not in terminals | PASS |
| `tsc --noEmit` exit 0 with output shown | Claim only; empty stdout | Too weak to re-prove; no contrary artifact | PASS |
| Storybook story for every added component | `eam.stories.tsx` (`Phieu488271`), `model.stories.tsx` (`Layer1`). Preview wraps styles only; tabs use Zustand so they render without a provider. | Stories **exist on disk**. One happy-path story each. No loading / empty / error / partial / extreme. `build-storybook` was **not** required by the letter of this gate (“story exists”). | PASS |
| Playwright covers primary flow of both units | `e2e/memory-model.spec.ts`: funnel stages + `#488271` + HIT; no `chip FPGA board` / `Cue binding` / `BOARD`; DDR “Không vẽ page/block giả”; model pipeline string + context injection + Layer 1 14.2 ms + Research MAC `18.240`; forbidden copy; keys 7/8. Config includes both viewports. Last run passed. | Proves primary flows. Does **not** prove visible focus or reduced motion on these tabs. | PASS |
| anti-ui-slop finish gate | Headings name the job (`Phễu truy hồi`, `Ký ức được chọn`, `Nhật ký bộ nhớ`, `Đường xử lý`, `Thời gian từng bước`). `Xem chỗ ký ức vào mô hình` / `Về bộ nhớ` / `Mở sóng` navigate. Empty panels exist when retrieval/model events are missing. Design contract `audit/design-contracts/R4.md` exists. No Lorem / Feature 1 / mockup labels in these two files. | Easy KPI sub `Không vẽ 800.000 ô` and DDR “Không vẽ page/block giả” are design-rationale copy (§32.7). `Waterfall` rows on Tab 8 are `<button>`s with no `tab`, so click does nothing. Cue panel is a single circle + id. Residual slop, not a fake cue table. | PASS — residual recorded |
| No placeholder / mockup copy (§32.1–32.8) | Source grep of the two tab files + e2e `FORBIDDEN` list on both tabs | Proves absence of the listed strings on the fixture screens | PASS |
| Every displayed metric has a provenance badge (§25) | Tab-level `EvidenceBadge` on `retrieval.provenance` and first/layer1 `modelEvents.provenance` (fixture `SYNTHETIC`, hairline / non-authoritative). Stage KPIs put `durationMs.provenance.source` or MAC in `sub`. Event rows badge each memory event. `Kpi` is not `EvidenceBadge`. | Incomplete vs “every metric is a badge”. Event-level SYNTHETIC is present; no BOARD fill on these units. | PASS — incomplete, not contradicted |
| Chart types match §8.1 | Memory candidate reduction → `Funnel` (log-width bars + vi-VN counts). Layer runtime → `Waterfall`. Occupancy / density map **withheld**: `awaiting("ddrUsage")`, no `DensityMap` / `sin()` cells. | Types match where data exists. Occupancy is AWAITING, not a fake density map. | PASS |
| Vietnamese copy §30; forbidden `bộ não AI` / `AI suy nghĩ` / `ý thức` | Absent in both tab files; e2e asserts the three phrases. Model copy: `không phải “AI đang nghĩ”` / insight `Không phải suy nghĩ.` | Proves | PASS |
| Keyboard, visible focus, reduced motion on both units | Keyboard: spec presses `7` then `8`; `TABS` index 6/7 = `eam` / `model`. Focus: global `:focus-visible` in `styles.css`; **no** R4 assertion. Reduced motion: global `@media` in `styles.css`; `e2e/shell.spec.ts` exists but is **outside** `testMatch`. These tabs have no local pulse. | Keyboard proven. Focus / reduced-motion **inherited, not verified this turn**. | PASS — residual harness |
| Rendered at 1440 and 1280 | Two Playwright projects; last run passed | Proves tests ran at both viewports, not a visual screenshot audit | PASS |

---

## Honesty audit (parent claims)

| Claim | Evidence | Result |
|---|---|---|
| Eam rebuilt from `sessionView.retrieval` + `memoryEvents` | `eam.tsx` reads those fields only. Funnel = recorded `stages` + optional `Episode #${selected}`. Does not import `funnel`, `eamQuery`, `MemoryGrid`, `DensityMap`, `memCells`. | **Proved** |
| No MemoryGrid / no `sin()` DensityMap / no BOARD cue table / no invented cue words | Tab source has none of those components. Cue / học-tại / payload render `không ghi`. E2E `Cue binding` and `chip FPGA board` count 0. `DensityMap` in `viz.tsx` still uses `Math.sin` but is unused by these units. | **Proved** on the two tabs |
| DDR occupancy AWAITING (`ddrUsage`) | `awaiting("ddrUsage").why` / `.needs`. Why text: reply 20 byte has no external-memory field. | **Proved** |
| Event stream kinds from contract | Table renders `e.kind`. Fixture kinds `READ`/`HIT`/`UPDATE` ⊂ `MemoryEventKind` (`READ WRITE HIT MISS INSERT UPDATE EVICT`). | **Proved** |
| SYNTHETIC badges; never BOARD | `EvidenceBadge` + `INTERACTION.activeSource` `SYNTHETIC` (`live: false` → warn pill). E2E exact `BOARD` count 0 on Bộ nhớ. Fixture `SYNTHETIC` on retrieval/memory/model. | **Proved** |
| Model from `sessionView.modelEvents` | Pipeline `join(" → ")` of `e.stage`. Waterfall `durationMs.value`. Layer 1 card: ms / sat / DDR B / MAC / norm / stalls / layerIndex from the recorded event. Omits SPEC example `128 chiều` (no dim on the contract event). | **Proved** |
| Memory context Episode #488271; copy says not thought | Fixture `contextEpisodeId` on stage `Memory context`. UI `Episode #488271 → Context → Model`. Caption denies thought. | **Proved** |
| TabId `model`; eam label Bộ nhớ; STAGE/PHASE_TAB MODEL → model | `data.ts` `TabId` + `TABS` + `STAGES`. `overview.tsx` `PHASE_TAB.MODEL = "model"`. `shell.tsx` `ICONS`/`VIEW`. `insight.tsx` `ModelInsight`. | **Proved** |
| 03E has no episode memory / no LM; fixture SYNTHETIC | `metrics.ts` `eamHitRate` / `tokenPerSec` awaiting copy. `data.ts` comments. Insight repeats 03E has no associative memory. | **Proved** as labeling; the **fixture still invents** 800k / #488271 / LM pipeline for the UI. That is allowed only as SYNTHETIC. |
| No occupancy invented | DDR panel is text-only awaiting. | **Proved** |
| Cue/payload/learned-at marked không ghi when absent | Episode card: `không ghi trong tương tác này` / `không ghi — chỉ có mã episode`. | **Proved** |
| No rtl / vivado / bit / UART / LiteScope hardware | Inspected R4 units live under `web/glassbox/`. Workspace has **no git index**. No UART/ILA/LiteScope strings in `eam.tsx` / `model.tsx`. | **No contrary evidence** in the unit set; tree-wide hardware edits are not independently diffable |

---

## Repository law

Allowed tree for this product lane: `web/glassbox/`, `services/glassbox/`, `contracts/glassbox/`, `.cursor/agents/`.

Inspected R4 units and wiring:

- `web/glassbox/src/components/tabs/eam.tsx` (rebuilt)
- `web/glassbox/src/components/tabs/model.tsx` (new)
- `web/glassbox/src/lib/data.ts` (`TabId` / tab row / STAGE → model)
- `web/glassbox/src/components/shell.tsx` (`model` icon + view)
- `web/glassbox/src/components/insight.tsx` (`ModelInsight`; `MemInsight` for eam)
- `web/glassbox/src/components/tabs/overview.tsx` (`PHASE_TAB.MODEL → "model"`)
- stories + `e2e/memory-model.spec.ts` + `playwright.config.ts` + `audit/design-contracts/R4.md`

No silicon `*_PASS` / `BOARD_PASS` in these files. FE still fixture-only (correct). Old Next e2e files remain on disk and stay ignored by `testMatch`.

`a7-fpga-gate` hard stop L3 (GlassBox / ILA / LiteScope before Native V1 freeze) applies to the **encoder silicon lane**. This review does not treat GlassBox UI work as a silicon bypass, and does not grant any board claim.

---

## Skills the parent said it read

| Path | What it should have changed | What I see |
|---|---|---|
| `.agents/skills/anti-ui-slop/SKILL.md` | Design contract before code; finish gate (no filler, no inert control) | `audit/design-contracts/R4.md` present. Tab copy is specific. Residual: cue circle; inert waterfall buttons; design-rationale KPI subs. |
| `.agents/skills/a7-fpga-gate/SKILL.md` | No UART/LiteScope; no silicon PASS from UI | No hardware in these units; no silicon PASS. |
| `.agents/skills/wcag-accessibility-audit/SKILL.md` | Keyboard, focus, contrast, names | Keyboard 7/8 tested. Funnel/cue SVG have text or `aria-label`. Contrast not re-run. Focus / reduced-motion not asserted on these units. |
| `~/.agents/skills/find-skills/SKILL.md` | Discover skills | Process only; no product rule to check. |

I read the first three files for this review. I cannot prove the parent read them before writing the tabs.

---

## §15 / §16 coverage

Present: retrieval funnel from recorded stages + selected episode; episode card with id + withheld cue/payload/learned-at; HIT/MISS from event kinds; DDR occupancy withheld; memory event table (kind / episode / address / SYNTHETIC / waveform link); model pipeline string; layer waterfall; Layer 1 card from recorded ms/MAC/DDR/sat/norm/stalls; context `Episode #488271 → Context → Model`; thought denial.

Incomplete, not blocking this sign-off:

- SPEC episode card sample (`Học tại #932`, last access `#1842`) is **absent from the interaction record** and correctly shown as không ghi — do not fill from `data.ts` `episodes`.
- SPEC layer-card example `128 chiều` / `DDR 42 KB` is **not** used; recorded `43008 B` and no dim field.
- Multi-cue map is episode-id only (no cue list in the record).
- Optional research fields `context length` not shown.
- Default shell is interaction `1842` (HIT + full model). Interaction `1841` empty-model / MISS branches exist in the fixture but are **unstoried** and not the e2e path.

---

## Blocking findings

None for **TURN_PASS** of these two units.

---

## Residual (does not flip this verdict; still blocks system-level claims)

1. `build-storybook` not run; no `storybook-static`. Stories-on-disk satisfy the turn gate; they do **not** prove axe-on-build.
2. Each new story is a single fixture state. Empty retrieval / empty model / MISS (`1841`) branches are unstoried.
3. `contrast-check.mjs` not re-run.
4. `e2e/shell.spec.ts` (reduced-motion / focus) and leftover Next e2e files remain on disk; current `testMatch` ignores them.
5. Visible focus and reduced motion not re-asserted on Tab 7 / Tab 8.
6. Per-KPI `EvidenceBadge` is not universal.
7. `Waterfall` on Tab 8 is a row of buttons with no destination.
8. Easy KPI `Không vẽ 800.000 ô` is design-rationale copy inside the product screen.
9. `web/glassbox/src/lib/data.ts` still exports invented `funnel`, `eamQuery`, `memoryEvents` (BOARD-stamped), `modelLayers` (`dim: 128`), `episodes`. **Tab 7/8 do not read them.** Tab Episode (`episode.tsx`) still renders that invented cue/payload list — **out of R4 scope**, residual product lie if a user opens Episode.
10. Frontend not wired to backend (correct for this phase).
11. No git index: hardware-tree cleanliness is inferred from unit paths, not a diff.

---

## §36 (these two screens only)

Advanced, not product-complete: #2 researcher numbers on Layer 1, #5 provenance at event/tab level, #8 funnel without a 800k-node graph, #10–11 no thought claim / no TWIN-as-BOARD, #13 no mockup chrome on these two tabs (aside from the residual design-rationale subs).

Not met for the product: waveform (#3, #12), token traceback (#9), remaining tabs, Episode invented list.

---

**TURN_PASS**
