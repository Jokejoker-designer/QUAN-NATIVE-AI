{
  "name": "@glassbox/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Frozen typed contract shared by the GlassBox frontend and backend. Consumed as TypeScript source via path alias; there is no build step and nothing is published.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  }
}
