# WO — ASTRA-C3-HELD-OUT-01 (draft for Antigravity to tighten)

Status: DRAFT. Antigravity must hunt leakage before Cursor implements.
Do not treat this file as permission to start RTL until Antigravity marks READY.

## Unknown (one)

Does the shared 32-feature learner, on the frozen C1 query law + C2 persist identity, improve held-out queries whose **entities are disjoint** from training, versus frozen / shuffled / per-ID controls?

## HIT letter (Master V1.1 §9)

```text
seeds >= 5, archived, not identical-φ plants
arms A / B / C / D all run on the same frozen train/held-out worlds
train entity set ∩ held-out entity set = empty
gain(A over B) >= 10 percentage points
paired CI lower bound > 0
A > shuffled-reward arm
A > or meaningfully complements per-ID prior
host winner / host address / host weight update = 0
Always-UNKNOWN = FAIL
n_false persist-style success = 0 if persist is in the path
```

## FAIL if

- A09R8 `w0 0→-5` is the transfer evidence
- ASTRA-07 / F3 historical bags are relabeled as this bag
- Held-out entities appear in train
- Only arm A is run
- Gold edited after xvlog
- C1 KEEP retrieve RTL edited
- C2 KEEP persist RTL / mig.prj edited
- PROGRAM=YES
- RESULTS self-stamps C3 Master close or BOARD_PASS

## Retention after reload

`retention drop <= 5 pp` may be a **later named bag** (`ASTRA-C3-HELD-OUT-RELOAD-01`) that uses C2 persist KEEP as submodule. Do not mix “does learning transfer?” and “does persist preserve transfer?” in one unknown if the TB cannot separate them.

## Silicon

Not this bag. C7 phase 6 is the silicon microexam.

## PROGRAM

NO. Evidence class: XSIM.
Does not freeze `PERSIST_SCHEMA_VERSION`, `DDR_QUERY_BOUND_FINAL`, `LM06_BYTE256`.
