# WO template — copy to WORK_ORDER/<UTC>.md

```markdown
# WO — <BAG_NAME>

Unknown (one sentence):

HIT letter:

FAIL if:

Must not edit:
- C0 hashes in KEEP_HASHES.json
- C1 KEEP retrieve RTL
- C2 KEEP persist RTL / official mig.prj
- GOLDEN after first xvlog

PROGRAM = NO
Evidence class = XSIM | MIG_XSIM | POST_ROUTE | BOARD
Does not freeze = DDR_QUERY_BOUND_FINAL, PERSIST_SCHEMA_VERSION, LM06_BYTE256
Does not stamp = BOARD_PASS, ASTRA_NATIVE_AI_BOARD_PASS
```
