# Current unknown for DeepSeek consult — ASTRA-C3-HELD-OUT-01

Live LOOP: C1 CLOSED_XSIM cartesian; C2 CLOSED_XSIM AXI journal + MIG reload; C3 OPEN.
unblocked_item=ASTRA-C3-HELD-OUT-01. PROGRAM=NO. BOARD_PASS=REJECT.
Do not freeze DDR_QUERY_BOUND_FINAL or PERSIST_SCHEMA_VERSION.
Do not edit C0/C1/C2 KEEP. Do not treat A09R8 w0 0→-5 as C3.

## One unknown

Does the shared 32-feature learner, on the frozen C1 query law + C2 persist identity,
improve held-out queries whose entities are disjoint from training, versus frozen /
shuffled-reward / per-ID controls?

## Must HIT (Master V1.1 §9)

```
seeds >= 5, archived, not identical-φ plants
arms A learner / B frozen / C shuffled / D per-ID on the same frozen worlds
train entities ∩ held-out entities = empty
gain(A over B) >= 10 pp; paired CI lower bound > 0
A > shuffled; A complements per-ID
host winner/address/weight update = 0
Always-UNKNOWN = FAIL
```

Retention after persist reload is a LATER bag. Silicon microexam is C7 phase 6.

## Reasoner task

Write a leakage-proof WORK_ORDER: DUT strategy (instantiate KEEP, no KEEP edit),
TB CLASS_* names, fail routing, what must not be compiled as DUT.

## RTL task

Draft named SV/TB skeletons only (DRAFT). One bag, one unknown. No live-clone paths
as if already accepted. No GOLDEN regeneration. No BOARD claim.
