# DeepSeek task — C3 through C6 (drafts only, not Master close)

You are producing a **sequenced WORK_ORDER DAG** plus the **first implementable bag**.
Do NOT stamp BOARD_PASS, C3–C6 CLOSED, or freeze DDR_QUERY_BOUND_FINAL / PERSIST_SCHEMA_VERSION / LM06_BYTE256.

Live FACT:
- C1 CLOSED_XSIM cartesian; C2 CLOSED_XSIM AXI+MIG reload
- A09R8 w0 0→-5 is NOT C3
- A09R8 bit is NOT C5/C6
- KEEP C0/C1/C2 must be instantiated, not edited
- PROGRAM=NO until C6 unique bit + WNS>=0 + auditor ACCEPT + owner

## C3 Master §9 (NEXT implementer bag)

Unknown: shared 32-feature learner improves disjoint held-out vs frozen/shuffled/per-ID.
HIT: 5 seeds, arms A/B/C/D, entity disjoint, gain(A-B)>=10pp, CI lower>0, host winner=0.
NOT this bag: silicon microexam (C7), persist-retention (later named bag).

## C4 Master §10

Unknown: LM06 emits grounded tokens from materialized <QUERY>+<PROOF>, not class ID.
Prefer LM06-BYTE256. Ablations: zero weights, evidence removed, evidence replaced.
Host next-token=0. Historical 653/689/237/60 oracle is NOT this law.

## C5 Master §11

Unknown: one production top with UART, role parser, real MIG index, persist, LM06.
Must be absent: plant-as-corpus, qid→answer, host winner, shadow path.
One DDR arbiter. Unified regression list in §11.

## C6 Master §12

Unknown: THAT exact top fits Arty A7-100T with WNS>=0 TNS=0 WHS>=0 UNROUTED=0.
A09R8 bit cannot close C6. Freeze manifest after unique bit. No RTL mutation after bit.

## Output contract

1. Ordered bags, ONE unknown each, names `ASTRA-C3-…` … `ASTRA-C6-…`
2. For each bag: HIT letter, FAIL routing, KEEP files untouched, forbidden DUT
3. Explicit: which bags are XSIM vs POST_ROUTE vs BOARD
4. First bag only: named DUT/TB file list (skeletons). Later bags: interface notes only
5. End with: C3–C6 MASTER_CLOSE=NO until raw evidence exists

Evidence classes: XSIM < MIG_XSIM < POST_ROUTE < BOARD. Never promote.
