# E1a — Direction NEW GATE (contract only)

`PROGRAM=NO`. **No parser/C3 RTL in this bag.** Existing F SVO-as-written stays
`2'd0`. Reverse is a **new capability**, not KEEP wiring.

Do not implement until E3a top-N is not occupying the only BASIC license
*and* this contract is independently reviewed. Mixing E1 RTL with E3
physical in one run is forbidden.

See `E1A_CONTRACT.json`, `E1B_PROOF_ENDPOINTS.json`, `E1C_DICT_LOCK.json`.
No RTL in this bag. `25_C5_EDGE_DIR_V1` is a sibling TB, not this gate.
