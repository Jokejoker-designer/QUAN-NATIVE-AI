# PREREG — ASTRA-11 timing close

```text
CHANGE = MMCM 100→50 MHz on pipe/UART (CLK100MHZ pin still E3 10 ns)
PASS   = WNS>=0 and WHS>=0, route complete
PROGRAM= NO until WNS>=0 (then still owner ASTRA-13)
```
