# PREREG — ASTRA-C4-LM06-GROUNDED-GEN-01

PROGRAM=NO. Frozen before xvlog. Does not edit C0/C1/C2 KEEP, frozen LM-06
`tiny_gpt803k_core.sv` / `a7lm06_pkg.sv`, `a7ng_evidence_compose.sv`, or
`a7ng_astra_c4_lm06_byte256.sv`.
Does not freeze `LM06_BYTE256` in FINAL_CONTRACT.

## One unknown

Do generated BYTE256 tokens depend on **both** loadable LM weights and
materialized evidence (object/has-proof), with FPGA-internal feedback to EOS,
and host next-token choice = 0?

## HIT letter this bag may measure (XSim compact)

```text
path: evidence → compact LM start → token → feedback → EOS/MAX
host next-token = 0
normal weights: 8/8 supported first-token == evid_obj
zero weights: first-token == EOS (safe), not evid_obj
corrupt g_match: first-token != evid_obj
evidence removed: first-token == EOS
evidence replaced (obj 10→30): first-token == 30, not 10
a7ng_evidence_compose not compiled as DUT
```

Master 90%/95% on a large held-out language set is **not** this bag (802k LM06
still not DUT). This bag must not self-stamp `C4_MASTER=CLOSED`.

## Quality bound

Linear head V=64, features = {obj match, no-proof safe, step-EOS}, plus bias.
Not TinyGPT-802k. Not teacher-off 90% language. Ablations are the unknown.

Supported objs frozen: 10,11,12,13,14,15,16,17.
Unsupported: evid_has=0 (n=2).

## FAIL if

- KEEP hashes drift
- `a7ng_evidence_compose` or `tiny_gpt803k_core` compiled as DUT
- GOLDEN edited after xvlog
- PROGRAM=YES
- TB chooses next token
- C4 BYTE256 adapter edited
