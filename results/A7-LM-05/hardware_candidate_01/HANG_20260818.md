# Aborted ladder 2026-08-18 17:05–17:20

- Programmed SHA `B7E68295E3277D5761EC8E4F92D73714B13287398DA79D9E0627FDF19B83A1C1` (candidate-01). Startup HIGH.
- Tensor K257 / K511 / K513 first-try PASS.
- Upload printed through `393216/399360` (~201 s). No further host line for >10 min.
- Process `python tools/a7lm05_hardware_ladder.py COM12` (PID 104000) killed 17:20. No `ladder.json` (finally never ran).

## What is confirmed

- Last printed offset is the **start** of the last 32 KiB window, not a proven hang at byte 393216.
- Remaining payload after that print is 6144 B (~0.5–3 s at the observed upload rate).
- `wait_write_idle` after upload is 120 s and then raises. A 10+ min silence with the process still alive is **not** explained by that wait unless `serial.write` blocked (no write timeout on the first run).
- After upload the script is silent through spots, fold0 (180 s), and one_full `wait_idle` (600 s). That last wait is the more likely host-side explanation.

## What is not confirmed

- `FAIL_UPLOAD_*` at the last 6 KiB (same family as candidate-00 addr 129024) — possible if UART TX blocked, not proven.
- Candidate-02 bit does **not** exist. C01 RTL already preloads `mem_addr_u` before `mem_we`.
- Quality CE and LM-06 were not run.

No 24/7 power is required after evidence is on disk. FPGA SRAM loses the bitstream when unplugged.
