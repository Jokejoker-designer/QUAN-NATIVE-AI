# BÀN GIAO CÔNG VIỆC — A7-EAM-03E

**Từ:** Grok Build (`grok-build-plan`)  
**Tới:** Cursor / phiên sau / Anh Quân  
**Ngày khóa:** 2026-08-19  
**Authority:** `PATCH_DRAFT` — FPGA research only. Không gửi tin khách. Không tuyên bố BOARD_PASS.

Đọc file này trước. Chi tiết ngắn: `HANDOFF.md`. Audit Cursor: `LOGIC_AUDIT_2026-08-19.md`.

---

## 0. Nhận diện phiên

| Mục | Giá trị |
|-----|---------|
| Grok session | `019ffa1c-a65c-71e0-8521-7d285e7c2ffd` |
| Request id | `4f4841c0-de0f-487d-a5b1-56f33fa9b897` |
| Model | grok-4.6 · xhigh · sandbox off |
| Mesh conversation | `conv-d5ba7905f46a` |
| Folder làm việc | `D:\Jetking_sem4\SEM_4\arty-a7-online-lm` |
| Grok cwd (đừng nhầm) | `C:\Users\phant\.grok\downloads` |
| Transcript | `C:\Users\phant\.grok\sessions\C%3A%5CUsers%5Cphant%5C.grok%5Cdownloads\019ffa1c-a65c-71e0-8521-7d285e7c2ffd` |
| Resume Grok | `grok --resume 019ffa1c-a65c-71e0-8521-7d285e7c2ffd` |
| Board | Arty A7-100 `xc7a100tcsg324-1`, Digilent `210319BE776EA`, COM12 115200 |
| Vivado | 2026.1 @ `C:\2026.1\Vivado` |

**Không nhầm** với Thanh Hoài inject session `019fb8d8-aed0-72d0-951d-a4eabc5617c0`. Lane này là FPGA encoder, không soạn tin khách.

---

## 1. Câu chuyện khoa học (đã khóa)

02H `Q1P_NOGO`: hidden LM-06 **không** phải cue semantic. Không retune LM-06 / Q1 / HIT_MAX.

02M: multi-cue **exact bind** trên 01R frozen. Hai cue Hamming 29 → một episode. **Không** semantic, **không** generalization.

03E-A0: encoder riêng, **không** dán 01R/02M/LM-06. Host chỉ gửi UTF-8 + SAME/DIFF. FPGA forward + `d1`/`dH` + SignSGD trên `E` và `Wh`.

**A0 đã chứng minh plasticity** (có thể kéo geometry từng cặp):

- FPGA tự forward và update
- SAME có thể gần hơn
- RESEED xóa geometry
- Đổi nhãn thì geometry đảo
- xsim và silicon (seed `0x11111111`) khớp số nguyên dù timing fail

**A0 chưa chứng minh discriminative geometry.** Seed `0x22222222`:

```
SAME  2135 → 1487
DIFF  1679 →  229
M_L1  = 229 − 1487 = −1258
```

Law hiện tại: *can move pairwise geometry*, chưa *can reliably create margin*.

`dH` train-pair 5→3 **không** phải unseen retrieval. **A1 CLOSED.** Nối 01R lúc này sẽ giả retrieval.

---

## 2. Trạng thái khóa (không tuyên bố quá)

```
A7-EAM-02M                 FROZEN / BOARD_PASS
A7-EAM-01R                 FROZEN / BOARD_PASS   HIT_MAX=8  MARGIN_MIN=4
A7-LM-00…06                FROZEN / BOARD_PASS   không rebuild

A7-EAM-03E-A0              XSIM_PASS
                           SILICON_FUNCTIONAL_PASS_WITH_NOTES
                           TIMING_FAIL
                           SEED_ROBUSTNESS_FAIL
                           A1 CLOSED

A0.1-T                     XSIM_PASS (golden 32-step frozen)
                           TIMING_FAIL trên bit đã archive
                           S_DIST→S_DADD đã patch trên RTL live
                           xsim lại + impl mới: CHƯA xong
                           không BOARD_PASS
```

Claim 02M: FPGA-native post-bitstream multi-cue episodic binding + teacher-off exact recall.  
Erratum 02M: NACK sau teacher-off có thể giữ stale `hit=1`; authority = `kind=0x9E` + `nack_code=1`. Sửa 02M.1 nếu đụng UART — không retune 01R.

---

## 3. Số vàng (authority)

Law T: `eam03e-a0-signsgd-v1`  
`d1 = Σ (|hA−hB| >> 5)`  
Seed TB: `0x11111111` · strings `ALPHA` / `BETA.` / `OMEGA` · **32** bước (không dùng STEPS=24 làm oracle T).

| Phase | d1(AB) | d1(AC) |
|-------|-------:|-------:|
| After seed + prime | 3930 | 5362 |
| After 32-step BETA=SAME | **1093** | **2012** |
| After RESEED | 3930 | — |
| After 32-step OMEGA=SAME | 1574 | **451** |

File: `results/A7-EAM-03E/golden_a01t.json`  
Marker xsim: `A7EAM03EA01T_XSIM_PASS`

A0 silicon cũ (host **STEPS=24**) swap ra 1986/983 — đó là protocol 24 bước, **không** phải golden T. Host đã sửa `STEPS=32` trong `tools/a7eam03e_a0_silicon.py`.

---

## 4. Bitstream — giữ, đừng ghi đè 02M/01R/LM

| Artifact | Path | SHA256 | WNS / TNS | DSP |
|----------|------|--------|-----------|-----|
| 02M frozen | `build/out/arty_a7_eam02m.bit` | `DB3BC58A6CC697FD0C290F97B5D6AD171AE7721A6C8A1E2DB2E87C5A84CFE696` | +0.788 / 0 | 0 |
| A0 (bit gốc đã bị ghi đè `build/out`) | SHA only | `12DD690C9DE6175E9C7E807666ADAC09046C65CDAE97CA6C974D803CB8059783` | −1.891 / −990.6 | 0 · LUT 11010 |
| A0.1-T pacc | `results/A7-EAM-03E/a01t_pacc/` | `7E13CA749FC9189EBC9DACD0D73DEC43AA3B12F4B4D9CA50E7A749F3E9648421` | −0.563 / −5.218 | 0 · LUT 7692 |
| A0.1-T eupd **bit hiện có** | `results/A7-EAM-03E/a01t_eupd/` và `build/out/arty_a7_eam03e.bit` | `ADD9E46280A697FD40C46911F5E477EF5B3A02EF36FE8054F9642216951C2262` | **−0.119 / −0.407** | 0 · LUT 7653 · FF 7157 · BRAM 3 |

RTL snapshot **trước** patch S_DADD: `results/A7-EAM-03E/a01t_eupd/eam03e_core.sv`  
SHA snapshot (Cursor): `717025A88F12C22B356DD626651CC359E2D5533083ACC9FADF3086F7815B04EE`

`build/out/arty_a7_eam03e.bit` **chưa** chứa S_DADD. Bit eupd là pacc+MAC+EUPD only.

---

## 5. Việc Grok đã xong (phiên này)

### 5.1 02M đóng

- Contract `docs/contracts/A7-EAM-02M.md` FROZEN / BOARD_PASS
- Claim không semantic
- Erratum NACK stale hit khóa bằng `0x9E`

### 5.2 A0 encoder

- RTL: `rtl/eam/a7eam03e_pkg.sv`, `eam03e_core.sv`, `eam03e_uart.sv`, `rtl/board/arty_a7_eam03e_top.sv`
- TB: `tests/xsim/tb_a7eam03e.sv` + `run_a7eam03e.tcl`
- Host: `tools/a7eam03e_a0_silicon.py`
- Build: `vivado/tcl/build_a7eam03e.tcl`
- Kết quả: `results/A7-EAM-03E/{xsim.md,silicon.md,ladder_a0.json}`

UART: envelope `A5 cmd n payload xor`, reply 20×`5A`. PING ident `3A` `A0`. PAIR `0x23`. Không MAP/PROBE 01R.

### 5.3 A0.1-T pipeline (không đổi law)

| Attempt | Đổi datapath | Golden | Timing |
|---------|--------------|--------|--------|
| A0 | 64-wide pacc | pass | −1.891 / −990.6 · path pacc/h fanout |
| pacc | serialize 1-bit projection + MAC WMUL/WADD | identical | −0.563 / −5.218 · path `i→g mux→sat8→e_wd` |
| eupd | register `sgn_r` / `wdelta_r` trước sat8 write | identical | −0.119 / −0.407 · path `i→d1_acc` (S_DIST) |

### 5.4 Kế hoạch A0.2 (contract only — RTL chưa làm)

`docs/contracts/A7-EAM-03E-A02.md`

- TRAIN vẫn L1; cosine **EVAL only**
- Law mới = version mới, không gọi là A0.1
- L0 baseline SignSGD → L1 triplet hinge một transaction  
  `L = max(0, d(A,P)−d(A,N)+m)`  
  không PAIR SAME rồi PAIR DIFF riêng
- L2 cheap shift-norm chỉ khi log thấy norm collapse
- L3 cosine TRAIN chỉ khi L1+L2 vẫn fail `M_cos`
- Gate: `d_pos` giảm, `M_L1>0`, `M_cos>0`, worst-seed ≥ 0, không inversion
- Kidi bags: confirmation L, không retune T

---

## 6. Việc Cursor làm sau khi Grok pause

Audit + đúng **một** mục resume (T datapath), chưa A0.2 RTL, chưa program board.

1. `rtl/eam/eam03e_core.sv`: `S_DIST` đăng ký `ad`, `S_DADD` cộng vào `d1_acc`. Thứ tự i=0..31 giữ. Law không đổi.
2. Empty-B vào DIST: clear `d1_acc`/`dH_acc` (stale acc).
3. Host `STEPS=32`.

**Grok ACK (đối chiếu RTL live):**

| # | Cursor hỏi | Grok |
|---|------------|------|
| 1 | S_DIST→S_DADD giữ term order i=0..31 | **ACK.** `ad<=abs>>5` rồi `d1_acc+=ad`; i tăng sau add. Cùng thứ tự A0. |
| 2 | Silicon T dùng STEPS=32 | **ACK.** 24-step là A0 silicon cũ (swap 983/1986). |
| 3 | Seed `0x22222222` thuộc A0.2-L, không retune `E3_MARG` | **ACK.** |
| 4 | Sau xsim golden: chỉ impl; không BOARD_PASS | **ACK.** |

**Chưa xác nhận:** xsim lại sau S_DADD; WNS sau route S_DADD; silicon 32-step trên kit (board disconnected).

---

## 7. File cần đọc

| File | Vai trò |
|------|---------|
| `docs/contracts/A7-EAM-02M.md` | 02M frozen |
| `docs/contracts/A7-EAM-03E.md` | parent encoder |
| `docs/contracts/A7-EAM-03E-A.md` | A0 lock flags |
| `docs/contracts/A7-EAM-03E-A01.md` | T + L0 |
| `docs/contracts/A7-EAM-03E-A02.md` | contrastive, chưa RTL |
| `docs/architecture/LINEAGE.md` | lineage EAM vs LM |
| `rtl/eam/eam03e_core.sv` | live = eupd + S_DADD |
| `results/A7-EAM-03E/a01t_eupd/eam03e_core.sv` | frozen T trước S_DADD |
| `KIDI_TRAINING_LESSON_PLAN.md` | confirmation bag **sau** L1 freeze |

---

## 8. Cấm

- Ghi đè `arty_a7_eam02m.bit`, `arty_a7_eam01r.bit`, `arty_a7_lm*.bit`
- Glue LM-06 / 01R / 02M vào bit A0/T
- Host gửi hash / gradient / weight / address / winner
- Đổi tokenizer
- Silent-tune `HIT_MAX`, `E3_MARG`, `m` theo seed fail
- Mở A1 vì `dH` train-pair giảm
- Gọi A0.1-T là BOARD_PASS khi WNS < 0
- Đổi golden integer rồi gọi là “timing win”
- Đổi learning law trên cùng version T

---

## 9. Việc tiếp theo (đúng thứ tự)

1. **xsim** `tests/xsim/run_a7eam03e.tcl` trên RTL live (S_DADD). Golden phải giữ 3930/5362→1093/2012→3930→451/1574. Fail = regression, revert về snapshot eupd.
2. **Impl** `vivado/tcl/build_a7eam03e.tcl`. Gate T: WNS ≥ 0, TNS = 0, DSP 0. Copy bit + SHA vào `results/A7-EAM-03E/` trước khi ghi đè `build/out`.
3. **Silicon** khi cắm kit: program bit T, host STEPS=32, COM12. Khớp golden. Vẫn không BOARD_PASS nếu chỉ functional.
4. **L0 telemetry** (sau T đóng): PAIR thêm `n1`, `max_abs`, `mean_abs`, `dot`, `n2sq`. Host tính `M_cos`. TRAIN law **không** đổi.
5. **A0.2-L L1**: CMD `0x25` một transaction A,P,N + hinge. Ident PING `3A` `A2` khi L1 ship.
6. A1 chỉ sau A0.2 PASS worst-seed `M_L1≥0` và `M_cos≥0`.

Nếu T (S_DADD) vẫn WNS âm: pipeline tiếp `S_DIST` fanout, **không** nhảy sang triplet để “trốn” timing.

---

## 10. Công thức nhớ

```
M_L1  = d(anchor, DIFF) − d(anchor, SAME)
M_cos = cos(A,P) − cos(A,N)
```

Seed xấu hiện tại: `M_L1 = −1258`.

| Quan sát | Kết luận |
|----------|----------|
| `M_L1<0` nhưng `M_cos>0` | metric/norm đang lừa — chưa đổi law |
| cả hai < 0 | law thiếu repulsion |
| norms cùng sập khi distance sập | mới justify L2 shift-norm |

---

**Kết:** Bottleneck không còn là “FPGA có học representation không” — đã học. Còn lại: (1) đóng 100 MHz, (2) law có tạo margin ổn định hay kéo mọi thứ vào một vùng. A0.2 chưa code. Board lúc bàn giao: disconnected.
