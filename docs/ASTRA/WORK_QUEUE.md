# Parent work queue — Master V1.1 C-gates

Live: `docs/ASTRA/LOOP_STATE.json`. Independent audit (Grok does not write it):
`D:\FPGA\ASTRA_C1_INDEPENDENT_RETRIEVAL_AUDIT`

Parent does not ask.

1. **NOW:** Independent auditor = Antigravity. Handoff: `D:\FPGA\ASTRA_C1_INDEPENDENT_RETRIEVAL_AUDIT\ANTIGRAVITY_START_HERE.md`. Live implementer = Cursor. C2 persist is **CLOSED_XSIM** (auditor `20260907T1652Z`). Next implementer bag: C3 held-out (`ASTRA-C3-HELD-OUT-01`) only after Antigravity WORK_ORDER. Do not freeze `PERSIST_SCHEMA_VERSION` or `DDR_QUERY_BOUND_FINAL`. Do not stamp BOARD_PASS. Do not treat A09R8 `w0 0→-5` as C3.
2. Then `ASTRA-C1-STREAM-INTERSECT-02` at **N=256** — CAP_AFTER + sorted merge + rare-first; **late gold must hit**.
3. Then same stream law N=4096 **late-gold stress** (no prefix luck; gold_n>=1 retrieve class with SEARCH_INCOMPLETE = FAIL).
4. Optional DIR-FULL16 → PAGE-SKIP → `qse-v2-intersect-context-02`.
5. Restart 256→800k only on **one** frozen stream law + semantic corpus.

**REJECT:** freeze C1 / 800k / `CAND_CAP_FINAL` from `qse-v2-intersect-01` N=4096 (`SEARCH_INCOMPLETE` sentinel; cap-then-AND). Do **not** edit that bag. Do **not** N=16384 on cap-then-AND.

Then C3 held-out → C4 LM06 → C5 production top → C6 freeze bit → C7 board exam.

Never parallel two implementers. PROGRAM new bit only with WNS≥0 + auditor ACCEPT + owner.
Checkpoint SHA `e51bdca2…` test vehicle only. No V3.1 tree.
