# A7-LM-05 — 399,360-param 4-layer DDR depth scale

**Status:** **BOARD_PASS / FROZEN** (2026-08-18 C02)  
**Opened:** 2026-08-18 (user waiver after R4 negative result)  
**Closed:** 2026-08-18 — claim `ARTY_A7_399K_DDR_DEPTH_LM_BOARD_VALIDATED` (depth/oracle only; not 8-class CE)  
**Requires:** A7-LM-00/01/02/03/04 BOARD_PASS / FROZEN. LM-04 R5 grants only its bounded target-switch adaptation claim.  
**Authority:** `Revised Arty A7 Program Master.md` + explicit user open 2026-08-18  
**law_id:** `lm05-signsgd-v1` (**do not change**)  
**Claim (only after all gates):** `ARTY_A7_399K_DDR_DEPTH_LM_BOARD_VALIDATED`

**Do not:** overwrite frozen 00–05 bits or `mig.prj`; broaden this claim into 8-way retrieval/open-domain LM; add Adam/BF16/low-rank/teacher/ternary; let the host compute board CE / next-token / updates. LM-06 is authorized by WNS +0.332.

This contract is frozen before implementation. Geometry or law change needs a new `law_id` and a revision **before** a validation run.

## Relationship to the closed LM-04 baseline

R4 showed **no evidence** that schedule-only search under the current law produces robust 8-way last-token retrieval on the 100K model. That remains a sealed negative result. R5 later closed the hardware baseline with a separately preregistered, bounded target-switch adaptation claim; it did not reverse R4.

LM-05 is **depth**, not a second attempt to rescue that 8-class task. R5 proves DDR persist/reload, exact 100K forward/update folds, multi-token parity, K/requant, and first-try sequential K=513 on silicon. R5 WNS +0.157 closes LM-04 but does not meet the separate +0.20 timing authorization threshold.

## Frozen model

```text
Vocab              512
Context             64
d_model             96
Heads                4
Layers               4
d_ff               192
d_head              24
Trainable params 399,360 = 2Vd + Cd + L(4d² + 2 d d_ff)

INT8 persistent W in DDR (official AXI MIG)
INT16 activations
INT32 MAC / sat
same shifts / LN STE / sign-SGD as LM-04
```

## Purpose

First **4-layer** DDR-resident FPGA-updated Transformer on Arty A7. Required new evidence is depth and traffic, not compression:

- all four layers receive updates
- sequential tensor commands in one program (K257 → K511 → K513 **first try**)
- persist flush/reload of 399,360 B
- AFTER = 0 writes
- DDR roofline counters logged
- WNS ≥ 0, TNS = 0 to close; WNS ≥ +0.20 to authorize LM-06

## Quality (do not repeat the R4 trap)

Do **not** close LM-05 on unguarded median CE drop ≥5%.

If a retrieval task is used, TRAIN and confirmation must share the same rule, confirmation is frozen after recipe lock, and gates include `mean_last_loss` (not fake PPL) plus collapse:

```text
max_pred_class_fraction <= 0.50
unique_pred_count >= 4
pred_entropy_bits > 1.5
not constant-class
```

Quality confirmation is written in a later `A7-LM-05-CONFIRMATION.md` and frozen before the board quality run. Hardware gates above can be implemented first.

## Two close levels

| Level | Condition |
|-------|-----------|
| LM-05 BOARD_PASS / FROZEN | all conjunctive hardware (+ registered quality) gates PASS, WNS ≥ 0, TNS = 0 |
| LM-06 AUTHORIZED | LM-05 BOARD_PASS **and** WNS ≥ +0.20, TNS = 0 |

## UART

Same A5/0x84 family as LM-04. Host compares only.

## Bitstream

`build/out/arty_a7_lm05.bit` only. Do not overwrite `arty_a7_lm04*.bit` or frozen 00–03.
