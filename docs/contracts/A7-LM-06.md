# A7-LM-06 — 802,816-param DDR scale

**Status:** **BOARD_PASS / FROZEN** (2026-08-19 C3)  
**Opened:** 2026-08-18 after LM-05 BOARD_PASS (WNS +0.332 ≥ +0.20)  
**Closed:** 2026-08-19 — claim `ARTY_A7_803K_DDR_SCALE_LM_BOARD_VALIDATED` (scale/oracle only; not 8-class CE)  
**Requires:** A7-LM-00…05 BOARD_PASS / FROZEN  
**Authority:** `Revised Arty A7 Program Master.md`  
**law_id:** `lm06-signsgd-v1` (**do not change** after first validation bit)  
**Claim:** `ARTY_A7_803K_DDR_SCALE_LM_BOARD_VALIDATED`

**Do not:** overwrite frozen 00–05 bits, C0/C1/C2 LM-06 evidence, or `mig.prj`; add Adam/BF16/low-rank as the baseline; let the host compute board CE / next-token / updates; close on unguarded 5% CE. LM-07 is authorized by C3 WNS +0.359 but is **not started**.

Reuse: `docs/contracts/A7-LM-06-LESSONS.md`.

Same INT8 / INT16 / INT32 / sign-SGD numerical law as LM-05. Geometry change requires this new `law_id`. Low-rank is an optional parallel variant (`A7-LM-06-LR`), not this baseline.

## Frozen model

```text
Vocab              1024
Context             128
d_model             128
Heads                 4
Layers                4
d_ff                256
d_head               32
Trainable params 802,816 = 2Vd + Cd + L(4d² + 2 d d_ff)

INT8 persistent W in DDR (official AXI MIG)
INT16 activations
INT32 MAC / sat
same shifts / LN STE / sign-SGD as LM-05
```

Check: 2·1024·128 + 128·128 + 4·(4·128² + 2·128·256) = 262144 + 16384 + 524288 = **802816**.

## Purpose

First **~0.8M** native-training scale on Arty A7. Required new evidence is **scale stability** and mixed fwd/bwd DDR traffic, not a new optimizer:

- tiled W that fits XC7A100T with MIG + tensor (see `A7-LM-06-TILE.md`)
- persist flush/reload of **802,816 B**
- all four layers receive updates
- sequential tensor K257 → K511 → K513 first try in one program
- AFTER = 0 writes
- exact host-compare folds vs a frozen Python/`TinyGPT803k` oracle
- WNS ≥ 0, TNS = 0 to close; WNS ≥ +0.20 to authorize LM-07

## Quality

Do **not** close on unguarded median CE drop ≥5%.  
If a retrieval/adaptation task is used, freeze `A7-LM-06-CONFIRMATION.md` **before** that board run. Hardware + oracle gates may ship first.

## Two close levels

| Level | Condition |
|-------|-----------|
| LM-06 BOARD_PASS / FROZEN | all hardware + registered confirmation gates PASS, WNS ≥ 0, TNS = 0 |
| LM-07 AUTHORIZED | LM-06 BOARD_PASS **and** WNS ≥ +0.20, TNS = 0 |

## UART / bit

Same A5/0x84 family. Host compares only.  
Close bit: `build/out/arty_a7_lm06c3.bit`. C1 `arty_a7_lm06.bit` stays locked (persist_reload fail). Do not overwrite `arty_a7_lm05*.bit` or frozen 00–04.
